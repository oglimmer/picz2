# Implementation Plan: Closing Architectural Gaps

Companion to `upload-concept.txt`. Tracks the work needed to bring the
status quo in line with the concept document.

Status legend (update as work lands):
- [ ] not started
- [~] in progress
- [x] done

Last reviewed: 2026-09-05 (CI on GitHub Actions, three workflows under `.github/workflows/`: `server.yml` runs `./mvnw test -Drun.testcontainers=true` on Temurin 17 — the Ubuntu runner has a real Docker daemon, so the eleven Testcontainers-gated tests run there too; `web.yml` runs `npm ci`, lint, both type-checks, vitest and `vite build` on Node 24; `ios.yml` runs the two guard scripts and one `xcodebuild test` on Xcode 26.6 / `macos-26` with the iPhone 17 and iPad Pro 11-inch (M5) simulators, ad-hoc signed (`CODE_SIGN_IDENTITY=-`, empty `DEVELOPMENT_TEAM`), never `CODE_SIGNING_ALLOWED=NO`. Each workflow is path-filtered and also fires on `renovate/**` branch pushes, which is what `automergeType: branch` in `renovate.json` needs — until now nothing reported a check, so every `automerge: true` was inert. Actions are pinned by SHA; `helpers:pinGitHubActionDigests` keeps them so. First run: web green; server red because all ten Docker-gated tests assumed an S3 endpoint from the developer's shell — they now start a MinIO Testcontainer (`testsupport/TestObjectStorage`, `org.testcontainers:minio`), and `ProcessingJobLeaseTest` additionally gained `@ActiveProfiles("worker")`, `webEnvironment = MOCK` and a one-day dispatcher poll interval, which its comment had claimed all along; 301 / 0 / 1 skipped with the gate open, run against the sandbox's `dind`. The iOS job is a first attempt — the project was last saved by an Xcode 27 beta, and ad-hoc signing of the keychain access group on the simulator is expected but unproven.)

Last reviewed: 2026-09-05 (`hidden` is derived, not assigned — **D79**. A photo carries `hidden` exactly while it has no other tag: giving it any tag (`all` or a custom one, one photo or the whole album) takes `hidden` off in the same transaction, taking the last tag off or deleting that tag puts it back. `hidden` cannot be added by hand and a lone `hidden` cannot be removed by hand; both answer 400 with a message that says to tag the photo instead. Nothing already stored is rewritten. The three clients stop offering `hidden` as a toggle (web tag select, quick tags, tag-all dropdown; iOS one-photo and bulk sheets) but keep it as a filter. Server 301 tests / 0 failures, frontend 70 / 0. iOS not built here — no Xcode in the sandbox — the two Swift edits are a model property and two `filter` calls.)

Last reviewed: 2026-09-05 (The web app has a test suite. vitest + jsdom, `npm test` in `frontend/`, 68 tests: the pure utils — Basic-auth UTF-8 encoding, `formatDate`'s calendar days, byte sizes, tag counts, cookies, the day-and-region clustering including the camera-clock day cut and the "no chaining across a city" guarantee — and the composables that carry logic: `requestJson`'s three error shapes, the D78 login/restore/logout flow and the one-time password-to-token migration, the poller's `waitFor`, `buildSections`/`groupContextFor`, lightbox wrap-around, upload error copy, selection ranges, reorder moves and the refused-reorder reload, duplicate detection, bulk delete/rotate/tag tallies, and the album loader's cancellation. Test files live next to the code as `src/**/*.test.ts`; they are excluded from the app's `tsconfig.json` and checked by `npm run type-check:test` against `tsconfig.vitest.json`, so `vue-tsc` on the app never needs vitest installed. No component mounting yet — that would add `@vue/test-utils`. Note for the Mac: `package.json` and the lockfile gained `vitest` and `jsdom`; run `npm install` there once. The sandbox could not touch the bind-mounted `node_modules`, so the suite was run from a Linux-only copy installed in scratch.)

Last reviewed: 2026-09-05 (The web app's architecture items from `fable51_findings.md`, all nine. **D78**: the browser now holds a revocable session token instead of the account password — `session_tokens` (`V52`), `POST /api/auth/sessions`, `DELETE /api/auth/sessions/current`, a Bearer filter ahead of Basic, every session revoked on a password change; iOS keeps Basic and notices nothing. The five plain-JavaScript SFCs are `<script setup lang="ts">` now, so `vue-tsc` covers the whole app. `GalleryView.vue` went from 2917 lines to about 1050: its concerns live in `composables/gallery/` (album loader with cancellation, selection, bulk actions, reorder mode, duplicate mode, tag picker, tag-all, group editing, recording session, upload flow) and in five components (`AlbumHeader`, `PresentationControls`, `GalleryShelf`, `TagPickerPanel`, `GridSizePicker`). What the two galleries had in duplicate is shared: `useLightboxNavigation`, `usePlaybackControls`, `useMapViewMode`, `useDayRegionView`, `countTags`, `DayRegionSections`, `PresentationSectionList`, `PublicPhotoTile`. One status poller: `useProcessingPoller.waitFor` lets a bulk rotate await its batch, and rotate itself is `useFiles.rotateFile`. `useApi.requestJson` replaced ~30 hand-rolled fetch/parse/check sequences and checks `response.ok` everywhere, so a 401 is a message and not a JSON syntax error. `composables/README.md` writes down which composables are singletons and which are per-instance. One router guard, one cookie parser (`utils/cookies.ts`), one album load sequence with a generation counter so a fast album switch never shows the previous album. Verified: `vue-tsc` 0 errors, `eslint` 0 errors, `vite build` ok, server 289 tests / 0 failures. Not verified end to end: a real login through the new session endpoint — the frontend still has no test suite, which is the next open practice item.)

Last reviewed: 2026-09-05 (Web app read-through, findings appended to `fable51_findings.md`; the eight bugs it found are fixed, the architecture, dead-code and practice items are recorded there as open. The fixes: Basic auth is now built from UTF-8 bytes (`frontend/src/utils/basicAuth.ts`) — `btoa` encodes UTF-16 code units, the server decodes UTF-8, so any non-ASCII password could register but never log in from the browser; `useSlideshowPlayback` keeps its audio handlers and detaches them by reference instead of passing fresh arrow functions to `removeEventListener`, which removed nothing and stacked a set per play; `useCapabilities` no longer caches the fallback after a failed fetch, so one network blip stops hiding the map and TUS until a hard refresh; the `FAILED` processing status is gone from the frontend to match D76; two stale strings in `GalleryView` (the deleted macOS Share Extension, the renamed "Manage Album Tags" entry); `useAnalytics` checks `response.ok` on pause/reset; `useConfirm` settles a still-open dialog with `false` before drawing the next one instead of leaving that promise hanging; `formatDate` counts signed calendar days, so a future camera clock is no longer "Today" and 23:50 yesterday is no longer "Today" either. Still open and needing a decision: the plaintext password in `localStorage` re-sent as Basic on every call — the D44 reasoning applies to the browser too — and the split of the 2917-line untyped `GalleryView.vue`, which wants a test net first; the frontend has none.)

Last reviewed: 2026-09-05 (**D77** — the local-disk storage mode is gone. `storage.s3.enabled` no longer exists: `ObjectStorageService`, `StorageClientFactory`, the S3 client bean and `BucketBootstrapper` are unconditional, and every service that carried `Optional<ObjectStorageService>` plus a disk branch (`FileStorageService`, `FileProcessingService`, `SlideshowRecordingService`, `RecordingAudioService`, `StorageQuotaService`, `StorageBackendService`, both media controllers) now takes the bean and does one thing. Deleted outright: `LocalFileCleanupService`, `RangeRequestHandler` (MinIO does the range slicing; the JVM only proxies bytes), the `Path` fields on `FileServeInfo` and `RecordingAudioInfo`, the `recordings/` directory and the `STORAGE_S3_ENABLED` env in helm and compose. `file.upload.upload-dir` remains as scratch only — multipart staging, worker work dirs, ffmpeg temp. A row whose path is not an object key (a pre-migration leftover, of which production should have none) now answers 410 from the serve path instead of being read off a disk the api pod does not have. Why now: production and local dev (compose) have both run MinIO since Phase 3; the only thing exercising the disk branch was the unit tests, which mocked their way around S3 and so tested a mode nobody deploys. Those tests now mock `BackendStorage` and assert the keys that are actually PUT and GET. Net −755 lines, 277 tests green.)

Last reviewed: 2026-09-05 (Decisions on the review's open items. **D75**: the gallery language names stay one instance-wide pair, but only `ROLE_ADMIN` may rename them — any login could, and the change hit every user's gallery; reading stays public for the share page. Per-user names were considered and deferred until strangers run galleries here. The Vue app hides the "Narration languages" menu entry and panel for everyone else: `GET /api/auth/check` now carries `admin`, `useAuth` exposes it as `isAdmin`, and `AlbumsView` gates on it — a field that can only answer 403 confuses more than it helps. iOS does the same: `AuthCheckResponse.admin` is optional (older servers omit it, and absent means plain account), `SyncOptionsViewModel.loadAccountRole` reads it on appearance, and the "Narration Languages" row in `SyncOptionsView` is shown only when it is true. **D76**: `JobStatus.FAILED` is gone. It was written by an older dispatcher and never leased or listed again, so a FAILED row sat invisible for good; `V51` renames every such row to `DEAD_LETTER`, where the admin listing shows it. Deferred on purpose: `User.createdAt` stays `LocalDateTime` (not exposed, and a type change needs a live schema-validation run to prove itself); the two read-only controllers that inject repositories stay; the twin status enums stay. Decided and started on its own branch: removing the local-disk storage mode — production and local dev both run MinIO, only unit tests exercised it.)

Last reviewed: 2026-09-05 (Second pass over `fable51_findings.md`: the smaller bugs, the dead code and the duplication. Uploads now prove album ownership before any byte moves — `storeFile` and `registerTusUpload` used to PUT into the album's backend first and 404 in the insert transaction afterwards, so a foreign album id left an orphan in somebody else's bucket. `AlbumService.deleteAlbum` deletes rows first and storage second (best-effort, never rolls the rows back), and the storage pass now also names the narration's `audio/` keys, which the SQL cascade dropped the rows for but nothing ever freed; the backend is passed in explicitly because after the album row is gone nothing can say which bucket. `purgeOrphanedS3Objects` lost its `@Transactional` (it pinned a pool connection for a whole bucket listing), the TUS pre-create backpressure comparison is `>=` like the filter's, and a failed hash or duplicate check no longer leaks the `.multipart-tmp` staging file. Removed: `listFiles()`, every presigner (`S3Presigner` beans, `presignGet`, `presign-seconds`), `transfer(InputStream…)`, five unused injected fields, the Jackson 2 `ObjectMapper` bean (the one caller uses Boot's Jackson 3 `JsonMapper`). Deduplicated: the worker's five identical lease/workdir prologues, the four admin sweeps, the S3 client builder (`storage/S3Clients`), token minting (`util/RandomTokens`), and the two hand-rolled Basic-header parsers, which now read `UserContext`. `SlideshowRecordingService` answers 404 rather than 400 for a missing row, the `published` gate moved into `listFilesByAlbumByShareToken` beside the `hidden` one, `GET /api/albums` does a COUNT and a one-row cover lookup instead of loading every file of every album, and the five entities with collections use `@Getter`/`@Setter` instead of `@Data`. Left open, each needing a decision: `User.createdAt` as `LocalDateTime`, the instance-wide gallery language names, and the local-disk storage shim that only tests still exercise.)

Last reviewed: 2026-09-05 (Five bugs from a full server read-through fixed — **D74** for the one that needed a decision. `DELETE /api/files/{id}` and `PUT /api/files/reorder` were owner-blind: `deleteFile` loaded by id alone and `reorderFiles` only checked that the ids existed, so any login could delete or shuffle anybody's photos; both now go through user-scoped queries (`findByIdAndUserId`, new `findExistingIdsForUser`). `/api/admin/**` was `authenticated()` and every account carried zero authorities, so any user could run the S3 orphan purge with `dryRun=false` — **D74**: `users.is_admin` (`V50`, default false, set by SQL like the quota) grants `ROLE_ADMIN` in `CustomUserDetailsService`, and `SecurityConfig` requires it on `/api/admin/**`; everyone is `ROLE_USER`. `AlbumSubscriptionNotificationService` was still on the once-a-minute cron noted on 2026-09-01; it is back to `0 0 */6 * * *`. `ImageServeController` and `SlideshowRecordingController` caught `Exception` and wrapped it, which turned a 410 (purged original), a 503 (breaker open) and the audio-not-ready 503 into 500s; they now rethrow every `RuntimeException` and wrap only checked I/O. The full review, including the smaller bugs, dead code and duplication that were *not* fixed, is in `fable51_findings.md`.)

Last reviewed: 2026-09-05 (A presentation chapter can now stop without the next one starting — **D73**. `presentation_groups.end_file_id` (`V49`, nullable) names the last image still in the chapter; every pre-existing row keeps the old open-ended behaviour. Both section walks — `usePresentationGroups.buildSections` and `PresentationGallery.sections` — close the open chapter when they reach its own end and fall back to a headingless run. Moving the end has its own endpoint, `PUT /api/presentation-groups/{id}/end`.)

Last reviewed: 2026-09-03 (The iOS app now also builds and runs on a Mac, via Mac Catalyst. `SUPPORTS_MACCATALYST = YES` on both the `Zyncloud` and `ShareExtension` targets — the extension needs it too, or the Mac build refuses to embed an iOS `.appex`. No source change was required: of 42 SwiftUI view files only five touch UIKit, and every API they use exists under Catalyst. The one behavioural fix is `DeviceIdentity`, because `UIDevice` misreports on Catalyst on purpose — `model` says "iPad" and `systemVersion` gives the paired iOS release, both of which were going straight into the server's device list. `ProcessInfo` is not remapped there, so it is the honest source; `isMacCatalystApp` is false on iOS, so there is one code path and no `#if`. A second, *native* macOS app was prototyped alongside this and is deliberately untracked — see the `macos/` rule in `.gitignore`.)

Last reviewed: 2026-09-03 (The iOS app can pause phone uploads, like the web app already could — **D71**. No server or schema change: the pause switch is and stays the *absence* of a target album, so the app sends the same `DELETE /api/settings/target-album` the web app's "Pause uploads" sends, and the sync side needed nothing — `SyncCoordinator.syncTargetAlbumFromServer` already dropped the local selection on a null answer. It is the picker's `nil` row rather than a separate toggle, because a second control over one server setting is a second thing to keep in step. Two client bugs fell out of it: the options screen used to fall back to the last local album when the server answered "paused", and the picker's `onChange` then read that programmatic assignment as a tap and `PUT` the album back — so opening Sync Options silently resumed a sync the web app had just paused. The picker now writes through an explicit `Binding` setter, which only a real tap runs.)

Last reviewed: 2026-09-02 (New uploads no longer publish themselves — **D70**. `users.new_asset_tag` (`V48`) says which tag a newly registered asset gets, `hidden` or `all`, and it defaults to `hidden` for every account including the existing ones. A `hidden` asset is stripped from the public album listing, from the public single-image page and from subscription mails; the owner publishes it by removing the tag. Switching the setting to `all` needs a confirmation the server checks for.)

Last reviewed: 2026-09-02 (Photos carry an owner-written caption, read by public visitors in the grid and in the zoomed view — **D69**. One `caption` column on `file_metadata` (`V47`), returned on the existing `FileInfo`, written through `PUT /api/files/{id}/caption`. It is a caption, not a comment thread: nobody but the album owner can write one, so the share link gains no spam surface.)

Last reviewed: 2026-09-01 (The `no_tag` marker is retired and the `all` tag now goes on every newly registered asset — **D68**. `FileStorageService.addAllTagToFile` attaches it on both upload paths and `AlbumService` on an album duplicate; `V46__drop_no_tag.sql` deletes the old marker's rows. Assets uploaded before the migration are **deliberately not backfilled**, so in an existing album the `all` filter covers only what arrives from now on.)

Last reviewed: 2026-09-01 (The iOS album and photo grids gained the web app's three-step size switch — **D67**. `GridSizeMode` plus a `size:` argument on `AdaptiveGrid`, reachable from the toolbar menu on both screens and from a pinch on either grid. `.medium` reproduces the counts both grids drew before, so nothing moves on upgrade.)

Last reviewed: 2026-09-01 (A migration path from picz v1 into this codebase — **D66**, runbook at `bin/migrate-v1.md`, **not run yet**. `bin/migrate_v1.py` moves one account's albums and photos out of the v1 deployment (`picz_prod` on the same MariaDB, images in AWS `s3://picz-images-bucket/images/`) and into v2. Scoped to `oglimmer@gmail.com` — v1 user ids 7 and 89, the same address through Keycloak and through Apple — which owns 3459 of v1's 3629 photos across 19 non-empty albums; the other 72 accounts are out of scope because v1 authenticates against OIDC and v2 against `users.password`, so none of them can log in here without a reset anyway. It writes rows and MinIO objects directly rather than going through `POST /api/upload`, for the same reason. Two phases, because `ImageResizeService.removeExif` means the v1 objects carry no EXIF at all and `FileProcessingService` writes `gps_*` unconditionally from what it extracts, `NULL` included: upload and let the worker build derivatives first, then backfill date and GPS from the v1 database. Timezones need no correction — the v1 api pod runs in UTC, so metadata-extractor's zone-less `getDateOriginal()` stored the camera's wall clock as a naive UTC datetime, written through with offset 0. Idempotent on `content_id = 'piczv1:<v1 element id>'`, which is also the rollback handle. 182 `SECTION` and 68 `MAP` elements are dropped as having no v2 equivalent, and 268 per-photo captions with them — `file_metadata` has no caption column. A 5-photo smoke test is live and left in place: album 47, files 6905–6909, verified identical to v1.)

Last reviewed: 2026-09-01 (Dead code swept out of all three components — **D65**. Every server route was matched against the paths the Vue app and the iOS app actually request, and nine had no caller in either, in helm, in scripts or in the transactional e-mails: `PUT /api/albums/reorder`, `POST /api/upload/multiple`, `GET /api/device-tokens/my-tokens`, `GET /api/files/{filename}`, `GET /api/recordings/{id}`, `GET /api/recordings/{id}/audio`, `GET /api/r/{publicToken}`, `GET /api/storage-backends/{id}` and `POST /api/public/subscriptions/unsubscribe`. Two near-misses are worth recording because they look dead and are not: `GET /api/public/subscriptions/unsubscribe` is reached only from `EmailService`'s one-click link, and `GET /api/sync/uploaded-checksums` is called by iOS — which writes its paths *without* a leading slash, so a `/api/` grep misses the whole client. The seven `/api/admin/*` routes stayed: no client calls them by design, they are operator curl documented in `helm/photo-upload/README.md`. Also removed: 22 Spring Data query methods whose names occurred exactly once in the repo, `SlideshowRecordingImageRepository` (declared, never injected), four other unreachable server helpers, three frontend files including `utils/image.ts` — client-side canvas rotation superseded by `POST /api/files/{id}/rotate` — seven composable members that were returned but never destructured, and four Swift declarations. ~1000 lines. Server 256 tests and the iOS suite both green afterwards. One live bug surfaced on the way and is **not** fixed here: `AlbumSubscriptionNotificationService` is annotated `@Scheduled(cron = "0 * * * * *")` under a comment reading "Run at minute 0 of every 6th hour" — subscription notifications are being processed every minute in production.)

Last reviewed: 2026-09-01 (The standalone macOS client `UploadExtension/` is deleted — **D64**. It was a separate Cocoa app, `PhotoUploader.app`, plus a macOS share extension, and it had been orphaned since **2026-05-04**: two commits in its entire life, no build in `oglimmer.sh`, no CI, no Helm, no mention in `README.md` or `CLAUDE.md`, and a `PRODUCT_BUNDLE_IDENTIFIER` still reading `com.example.PhotoUploader`. The iOS code review of 2026-08-21 had already named it a dead macOS clone (§10). It also missed the D63 rename, still showing "Upload Photos" and "Photo Uploader". Note that it was not *broken*: it sends TUS auth as plaintext `email:password`, which `TusHookService.authenticate` still accepts alongside the D44 scoped token, and it sends no `checksum`, which D61 treats as optional — so it would still have uploaded. It was removed for being unmaintained, not for being incompatible. Its 61 tracked files plus 197 MB of gitignored `build/`+`release/` output are gone; the now-dangling `UploadExtension/` line in `.dockerignore` went with them. Nothing replaces it — the iOS app is `TARGETED_DEVICE_FAMILY = "1,2"`, iPhone and iPad only, so macOS has no native client at all now; the web UI is the macOS story. Recover with `git checkout 1a2265b -- UploadExtension` if that ever changes.)

Last reviewed: 2026-08-31 (Product name unified on **Picz** and the version bumped to **2.0.0** — **D63**. The name had drifted into four forms: `Picz` on the landing page, tab title and app headers; `Picz2` on the auth pages, the Terms/Privacy/Imprint pages and every transactional e-mail; `Photo Gallery` as the `og:site_name` on shared-album and shared-image previews; and `Zyncloud` as the iOS `CFBundleDisplayName`, its two permission prompts and four in-app permission-error strings. All of them now read `Picz`. The iOS share extension's display name (`Photo Upload` in its plist, `ShareExtension` in the two `INFOPLIST_KEY_CFBundleDisplayName` build settings) is `Picz` too, so the share sheet matches the home screen. Identifiers were deliberately left alone: the `ios/Zyncloud/` directory, the Xcode target and scheme names, `PRODUCT_BUNDLE_IDENTIFIER`, the `com.oglimmer.photosync` keychain service and the `PhotoUploadCredentials` legacy service all still say Zyncloud/PhotoUpload, because renaming them would orphan the keychain entries every logged-in device depends on. `picz2.oglimmer.com` and the `picz2-be`/`picz2-fe` image names are unchanged — they are infrastructure, not brand. Version 2.0.0 was written by hand into `VERSION`, `frontend/package.json`, `frontend/package-lock.json`, `server/pom.xml` (kept `-SNAPSHOT`) and both `MARKETING_VERSION` settings; `oglimmer.sh release` was NOT run, so there is no commit, no `v2.0.0` tag, no `CURRENT_PROJECT_VERSION` bump and no deploy. A later `oglimmer.sh release` would bump *from* 2.0.0, so pick `patch` for the first real 2.x release, not `major`.)

Last reviewed: 2026-08-31 (The TUS **create** POST moved onto the background URLSession — **D62**. It was the last upload request still on `URLSession.shared`, so iOS killed it whenever the app was suspended and delivered "The request timed out." at the next wake-up, against a server that had never seen the request. Confirmed against the cluster: tusd logged zero errors and zero non-2xx over the failing window, and the failing requests never arrived. Three of those in a row tripped `ExportRetryPolicy`, which then blamed *export* for a request that was never sent — the same misattribution **D56** fixed for the Wi‑Fi Only case, from a different cause.)

Last reviewed: 2026-08-31 (TUS uploads carry a checksum, so two devices stop duplicating one library — **D61**. The TUS path deduplicated on `contentId` only, and on iOS a `contentId` is a `PHAsset.localIdentifier` that an iPhone and an iPad mint separately for their own copy of the same iCloud photo; TUS rows also never had `checksum` set at all. Since `TUS_ADVERTISED=true` routes *every* asset down that path regardless of size, an account synced from two devices duplicated everything. The client now sends its SHA-256 as `Upload-Metadata.checksum`; the pre-create hook rejects a known one with 409 before tusd allocates the upload, post-finish re-checks it to catch two devices racing, and `registerTusUpload` persists it. Scoped per user — unlike the multipart path's account-blind `findByChecksum`. Existing checksum-less rows are unaffected; they were cleaned up by hand.)

Last reviewed: 2026-08-30 (Per-user storage quota on the instance's own MinIO — **D60**. `users.storage_quota_bytes` (V45), 100 MiB by default and changed only by an `UPDATE`. Only the system backend is metered; an album on the user's own S3 is uncapped, and the `tus-uploads/` staging prefix is never counted, so pass-through is free. Originals, derivatives (`file_metadata.derivative_bytes`) and narration (`slideshow_recordings.audio_bytes`) all count, deduplicated by storage key so a duplicated album does not double the figure. Refusals are 507 at TUS pre-create, TUS post-finish, multipart upload and narration upload. Existing rows meter as free until `POST /api/admin/recalculate-derivative-bytes` is run once.)

Last reviewed: 2026-08-30 (Per-album storage, "bring your own S3" — **D59**. `storage_backends` (V44) plus `albums.storage_backend_id`; a user registers their own S3-compatible endpoint and picks it when creating an album, and cannot change it afterwards. The system default is a credential-free row that resolves `storage.s3.*` at runtime. `ObjectStorageService` became a router — `forAlbum` / `forFile` / `forSystemDefault` hand back a `BackendStorage` bound to one bucket — and the TUS finish hook now transfers rather than copies when the album is not on the system default. Retention and the orphan sweep run one backend at a time. Needs `storage.backend-secret-key` (`objectStorage.backendSecretKey` in the chart) or the feature stays off.)

Last reviewed: 2026-08-30 (Follow-up to **D58**: the first `helm upgrade --reuse-values` that turned `apns.enabled=true` shipped an **empty** `APNS_TOPIC`, because `--reuse-values` bases on the previous release's values and never picks up a new chart default. APNs rejects every push without a topic while the api still logs a cheerful "client initialized", so the failure was silent in both directions. `apns.topic` is now `required` in the template, and the runbook says to pass every field explicitly. Also set `apns.production=true` to match `Zyncloud.Release.entitlements`, which carries `aps-environment: production` — sandbox would have answered `BadDeviceToken` for any TestFlight build.)

Last reviewed: 2026-08-30 (APNs key `W4CMQFDV9D` revoked in the Apple portal and replaced by `289ZRKLFNQ`, which has never been committed — completes **D58**. The revoked key is still in git history at `server/src/main/resources/AuthKey_W4CMQFDV9D.p8`; it is dead, so the history is left alone rather than rewritten. Defaults and the Helm runbook now name the new key id.)

Last reviewed: 2026-08-29 (APNs signing key out of the repo — **D58**. The key was committed at `server/src/main/resources/AuthKey_W4CMQFDV9D.p8` in the very first commit and read off the classpath, so it was baked into every image and could not be rotated without a rebuild. It now arrives as a PEM body from the chart's secret, exactly like the MapKit key, and the file sits gitignored at the repo root beside it. Deploy order matters: set the secret before rolling the new image, or push goes quiet.)

Last reviewed: 2026-08-29 (Publish gate for albums — server, web and iOS. **D57**: a new album was public the moment it was created, because `createAlbum` mints the share token, so the link worked on an empty album and the subscription notifier could mail people about a half-filled one. `albums.published` (migration `V43`) now gates every share-token route and the notifier; existing albums were backfilled to published so no live link broke. New-album notifications key off `published_at` instead of `created_at`.)

Last reviewed: 2026-08-29 (iOS client only, no server or schema change. **D56**: with Wi‑Fi Only on and the phone on cellular, every upload attempt was refused locally by `applyNetworkPolicy()` and surfaced as a red "Failed to upload …: The Internet connection appears to be offline" — plus a second red line blaming export, once three of them tripped `ExportRetryPolicy`. New `NetworkMonitor` gates `drainQueue()` on `NWPathMonitor`, so the queue is held rather than failed, the Status tab shows "Uploads paused / Waiting for Wi‑Fi", and the log gets one informational line per pause instead of an error per attempt.)

Last reviewed: 2026-08-27 (Map brought to iOS, in the album *and* in presentation mode — see **D49**. Reported as "the map entry is missing from Present"; it is missing from the web's Present too (`mapFilterAvailable` is gated on `!presentationMode`), so this goes one step beyond parity on purpose. `Views/Map/` is a port of `PhotoMap.vue` onto native MapKit: same 4-decimal (~11 m) bucketing into pins, same reliance on MapKit's own pixel-distance clustering — which is why it wraps `MKMapView` rather than SwiftUI's `Map`, since only the former offers `clusteringIdentifier` and hands back `memberAnnotations` on selection — same saved-view-wins-over-auto-fit rule, and the owner's "Save this view"/Reset against the existing `PUT|DELETE /api/albums/{id}/map-view`. Two deliberate departures: the map is **not** gated on `/api/capabilities.maps.enabled`, because that flag says the server holds a key for MapKit *JS* and native MapKit needs neither key nor token endpoint; and in presentation the filter is a `PresentationFilter` enum rather than the web's `__map__` sentinel, so the map cannot leak into the value that feeds commentaries and chapter markers. **Server unchanged.** iOS gains `PhotoMapPlacesTests` plus four map-view endpoint-shape cases.)

Last reviewed: 2026-08-27 (Presentation mode brought to iOS — see **D48**. The web gallery's "Present" had no counterpart on the phone, so an album's chapters could be written in the browser and never read where most people actually look at photos. `Views/Presentation/` adds the same read-only screen: pick a tag, and the album is shelved into the `presentation_groups` sections anchored on it, with the commentary recorded for that tag one tap away. The shelving is a port of `usePresentationGroups` into the pure `PresentationGallery` (Utils), so an album breaks into the same chapters on both clients — anchors, not membership lists, which is why a group whose anchor has been untagged or deleted simply stops rendering. The chapter marker follows the reader into the new full-screen pager rather than living only above the grid, because that is where a presentation is actually read. **Server unchanged** — the two `GET` endpoints already existed; the client is read-only, groups are still made in the web gallery. iOS suite gains `PresentationGalleryTests` plus three endpoint-shape cases.)

Last reviewed: 2026-08-26 (TUS uploads made genuinely resumable on iOS — see **D47**. The client had never sent anything but `Upload-Offset: 0`, so no upload slower than Traefik's 60s `readTimeout` could ever finish. Chunked PATCH from the server's offset, a background-session HEAD to discover it, a resume budget that counts consecutive stalls rather than a file's lifetime, and slicing moved off the URLSession delegate queue. Share-sheet batches now report partial success and pre-check the size cap. Server unchanged.)

Last reviewed: 2026-08-28 (iOS **Status tab** readouts explained, plus the options page simplified in two passes. **Status tab**: the "Current" and "Background Tasks" counters were honest but not self-explanatory — "In scope" reads as a to-do list when it is really "photos on this phone inside the sync window, most already backed up"; "Last sync" is when a file last *finished uploading*, not when the app last looked, so it sits still on a quiet day and looks broken; and "Refresh" vs "Processing" is a `BGAppRefreshTask`/`BGProcessingTask` distinction no user should have to know. Each section header now carries an ⓘ opening a glossary sheet (`StatusFieldGuideSheet`), rather than captioning every row — the tab is a dense table on purpose and per-row captions roughly double both sections. The wording lives in a pure `StatusFieldGuide` away from the view so it is testable: the "In scope" line interpolates the user's own `syncLastDays` (singular at 1), and the "Too big to back up" entry appears only when that row does. New `StatusFieldGuideTests` pins field coverage, order, uniqueness and the two sheet identities. **Earlier the same day**: options page simplified, two passes. **Permissions section**: it sat at the top of Sync Options permanently, reporting "Authorized" forever to the ~all users for whom that is the steady state. It now renders only when `authStatus != .authorized`. The interesting part is what that surfaced: `.limited` was coloured **green** and suppressed the action button, so "Photos handed us only the assets you hand-picked" looked identical to "everything is fine" — a background sync in that state silently skips most of the library. `.limited` is now orange, stays visible, and carries a plain-English line saying what it costs. Two smaller bugs fixed alongside: the "Request Access" button was offered to denied users, where `requestAuthorization` returns the same answer without presenting anything (only `.notDetermined` can still be prompted; everything else now gets "Open Settings" via `UIApplication.openSettingsURLString`), and the status was read once in `init`, so granting access in iOS Settings and coming back left the row stale — `checkPhotoAccess()` is now called on `.onAppear` and on every `scenePhase == .active`. New `PhotoAccessPresentationTests` pins all four derived properties. **Earlier the same day**: the `Settings.useTus` opt-in is gone. The switch gated a decision the server already makes: `UploadRouting.selectPath` required *both* the local flag and `/api/capabilities.tus.enabled`, and the server is ours, so the second gate only ever produced a way for a user to silently opt out of resumable uploads and back onto the path that restarts a 2 GB video from byte 0. Removed the "Advanced" section from `SyncOptionsView`, the `@Published useTus` + its `Snapshot` field + the `settings.useTus` UserDefaults key from `Settings`, and the `userOptedIn:` parameter from `UploadRouting.selectPath` — the capability alone decides, with the not-yet-fetched third state still falling back to multipart. Existing installs that had toggled it off are moved onto TUS on upgrade; the stale key is simply ignored. `tusMaxUploadBytes` stays — it is server-advertised state, not a preference. Closes one of the two remaining R4 blockers; the web multipart fallback in `useUpload.ts` is the last one. iOS suite green, 502 cases. Earlier: Audio commentary made reliable on iOS — see **D45**. The AAC sibling shipped earlier the same day was transcoded *inside* the audio request; on this cluster that runs for a minute, which `AVPlayer` will not wait through, so an old commentary failed on the first play and worked on the second. Worse, the connections `AVPlayer` abandons made the aborted stream throw into `@ExceptionHandler`, which tried to write JSON into a committed `audio/mp4` response and corrupted Tomcat's recycled header state — poisoning the *next* request, which is why re-entering the album reproduced it. The transcode moved to the worker as `JobType.TRANSCODE_AUDIO_AAC` (new profile-less `RecordingAudioService`; **V42** adds the `recording_id` column it needs, because `asset_id` carries a foreign key into `file_metadata` and a recording id cannot satisfy it — the first deploy failed on exactly that), the api answers **503 + `Retry-After`** via `AudioNotReadyException` while it is being made, and iOS polls the new permitAll `GET /api/r/{token}/audio/status` showing "Preparing this commentary for iPhone" instead of an error. Also fixed: `ObjectStorageService.exists` counted every 404 as a MinIO failure and could trip the circuit breaker, the streaming body now swallows the client-gone `IOException`, and the generic handler writes no body into a committed response. Browsers still get the Opus master untouched. **Separately, and worse — see D46**: chasing a 404 on one old commentary showed that `POST /api/admin/purge-orphaned-s3` deletes every bucket key with no `file_metadata` row, and recording audio lives in `slideshow_recordings`, not `file_metadata` — so one run of it deleted **40 of 45 narrations**, everything made before 2026-08-17, unrecoverably (the MinIO volume has no Longhorn backup job and its only snapshot postdates the loss). The sweep now unions the recording audio paths and their derived `.m4a` keys into the known set and skips the in-flight `tus-uploads/` prefix entirely, pinned by the new `FileStorageServiceOrphanPurgeTest`. Suites green: server 227, iOS builds clean. Earlier the same day: Audio commentary brought to iOS, plus an AAC sibling rendition on the server. The app can now record a narrated slideshow (`Views/Narration/`): pick a tag filter and a language, tap through the matching assets while the microphone runs, and the timings upload alongside the audio to the existing `POST /api/albums/{id}/recordings` — same wire payload as the web `useSlideshow`, so a commentary made on the phone plays back in the browser. Two format facts drove the design. **Upward**: iOS has no Opus encoder, so the phone records AAC but uploads it named `recording.webm`; the server derives the *stored* file's extension from that name and then hands the bytes to ffmpeg, which probes the real format from content — so the stored file really is WebM/Opus, identical to a browser-made one. **Downward**: Apple's media stack has no WebM demuxer at all (`AudioFileOpenURL` fails outright), so `AVPlayer` given the master never starts and reports nothing — the preview showed slide 1 forever and Play did nothing. Fixed by keeping Opus/WebM as the master for the web and adding an AAC/`.m4a` sibling: `AudioReencodingService.transcodeToAac`, `StoragePaths.aacFilename`/`audioAacKey`, made at upload for new recordings and on first request for older ones (`SlideshowRecordingService.aacRendition`, kept afterwards — one ffmpeg run per old recording, ever). Both audio endpoints take `?format=m4a`; browsers pass nothing and are untouched. `ObjectStorageService.exists` (HeadObject) is new; deleting a recording now also deletes the derived sibling, which is named rather than stored so no migration was needed. The iOS preview also observes `AVPlayerItem.status` and *says* when it cannot decode, rather than presenting a dead transport — that silence is what made the original bug so hard to read. Suites green: server 218, iOS all pass. Earlier: Video preview fixed on iOS: tapping a video in an album showed "Failed". `PhotoDetailView` asked for `/api/i/{token}?size=large` and handed the bytes to `UIImage(data:)` — but a video has no `large` derivative, so the server answered with the video itself and the decode returned nil. The detail view now branches on the new `FileInfo.isVideo` and plays videos in an AVKit `VideoPlayer` fed by `AlbumDetailViewModel.videoURL`, which carries **no** `size` parameter so the server serves the transcoded H.264 rendition when there is one (the same URL the web Lightbox uses); the audio session is set to `.playback` so the silent switch does not mute it, and video thumbnails now carry a play badge. Server side, `ImageServeController` gained HTTP Range support — AVPlayer opens with `Range: bytes=0-1` and will not seek in a response without `Accept-Ranges`; a ranged GET is now forwarded to MinIO (`ObjectStorageService.openStream(key, range)`, the shape `SlideshowRecordingController` already used for audio) or served from disk via `RangeRequestHandler`, and unranged responses advertise `Accept-Ranges: bytes`. **First deploy 500'd and the fix is the interesting part**: the range branch lived inside the existing handler, which is declared `ResponseEntity<?>`, and `StreamingResponseBodyReturnValueHandler` only claims a return value whose *declared* generic is `StreamingResponseBody` — so a wildcard fell through to `HttpEntityMethodProcessor`, which looked for a message converter for the lambda and threw `HttpMessageNotWritableException: No converter for [...$$Lambda] with preset Content-Type 'video/mp4'`. Every direct-call unit test passed through it. Ranged GETs now have their own mapping, `@GetMapping(value = "/{token}", headers = HttpHeaders.RANGE)` returning `ResponseEntity<StreamingResponseBody>` (Spring prefers it over the wildcard mapping because the headers condition is more specific), and the new `ImageServeRangeMvcTest` drives the dispatcher under MockMvc — the only level at which that class of bug is visible. It waits on `MvcResult.getAsyncResult()` before asserting, because a streaming body is written from a worker thread and asserting straight after `perform()` passes or fails by luck. Suites green: server 213, iOS incl. new `VideoPreviewTests`. Earlier: Share-extension UX, iOS-only, no contract change: the upload progress bar reported `uploadedCount / totalCount`, so sharing a single video sat at 0% for the entire upload and then jumped to 100% — two minutes that looked like a hang. The cause was `URLSession.shared`, which cannot carry a delegate, so `didSendBodyData` was never delivered; `UploadService` now owns a delegate session and a tested `ShareUploadProgress` that weights files by byte size (a 200 MB video and a 2 MB photo are no longer half the work each), holds the high-water mark so a retried body does not walk the bar backwards, retires a failed file’s weight so the bar cannot park, and throttles to per-mille steps; the sheet also shows the percentage. Second fix: the album picker always opened on `albums.first`, so the destination had to be re-picked on every share — new `LastAlbumStore` (the extension’s own `UserDefaults`; app and extension share a keychain group but no App Group, and the app keeps its own destination in `Settings.albumId`) plus a pure `AlbumPreselection.choose`, which falls back to the first album when the remembered one has been deleted, or the picker would open empty with Upload disabled and no explanation. Sign-out forgets it, since album ids belong to one account. iOS suite 228 → 244, green. Earlier: iOS second review pass, see `ios/Zyncloud/CODE-REVIEW-2026-08-21.md` §8 — four client-only fixes, no contract change: `SyncLogger` re-encoded its whole 100-entry buffer into `UserDefaults` on every log line on the main thread (now coalesced off-main, flushed on background, like `UploadStore`); `SyncCoordinator.cachedCapabilities` was read and written from three threads with no lock while `cachedApi` beside it had one; the thumbnail loader did a securityd IPC per grid cell to build a Basic auth header for `/api/i/**`, which is `permitAll`, and had no image cache at all; and `Uploader` labelled every non-video `image/jpeg`, so iPhone HEIC photos were mislabelled and survived only because the server also sniffs the filename extension — new pure `ExportFormat` reads `PHAssetResource.uniformTypeIdentifier` instead. iOS suite 211 → 228, green. Earlier: iOS review backlog cleared, see D44 and `ios/Zyncloud/CODE-REVIEW-2026-08-21.md` §5 — scoped upload tokens replace the account password in TUS `Upload-Metadata` (V41, both clients, legacy value still accepted so deploy order does not matter); reconciliation now runs on contentIds via the new `GET /api/sync/uploaded-content-ids`, which is the version that survives a reinstall; `UploadStore` moved off `UserDefaults` to one coalesced JSON file; the app no longer blocks the main thread for up to 4 s on every activation; TUS temp-file leaks, the nested-ObservableObject settings toggles, the per-call keychain read and PII in release logs are all closed. Suites: iOS 211, server 208, both green. Earlier: Video size ceiling partly closed, see D43 — the TUS cap moved from 500 MB to **2 GiB** and an oversized file now produces a readable refusal on both clients ("Too big to back up: IMG_4021.MOV is 2.4 GB, the server accepts up to 2 GB") instead of a bare HTTP 413, with the skip recorded against the limit that caused it so a later, larger cap un-skips it automatically. Client-side re-encoding is deliberately **not** done — the phone still uploads the untouched original. Newly exposed and still open: Traefik's default 60 s `readTimeout` caps how long the ingress will read a request body, and the client still sends the whole file in one PATCH, so nothing much above 300 MB completes on a normal connection until that entryPoint timeout is raised. Earlier: an ordinary 4:30 iPhone clip was refused with 413 because the iOS client uploads the untouched 4K original and tusd capped uploads at 500 MB. The library has never held a video above 137 MB, and the Toronto trip stored 397 photos but only 4 videos, so this is data loss rather than a storage-efficiency question. Not fixed: the entry records the full chain, including that the server transcode has no scale filter and that the client never resumes. Earlier: Day grouping cut on the wrong clock, fixed — see D41. "By day & region" read the calendar day out of a true instant with the *viewer's* timezone, so a Toronto album browsed from Frankfurt broke its days six hours early. V40 adds `capture_utc_offset_seconds`, written by every extractor that resolves an instant (EXIF `OffsetTimeOriginal`, the fallback zone's offset at that instant, QuickTime `creationdate`); the gallery adds it back to get the camera's own wall clock and cuts days there. No trip-timezone setting: the offset is inferred per file, and undoing a fallback offset recovers the original wall clock either way. Files that can never have one (mvhd-only video, purged originals) borrow the album's dominant offset. **Backfill required**: `POST /api/admin/reextract-capture-dates` until it returns 0. Same commit fixes an older bug the offset work surfaced: `duplicateAlbum` dropped GPS coordinates and left copies stuck at `QUEUED`, which greyed them out in the gallery and hid them from every admin sweep. Test suite is green again — 199 tests, 0 failures: the six long-standing red tests were all stale assertions against code that had since moved on, see D42. Earlier: Backend startup probe budget raised, see D40 — the api pod restart-looped on a node at 96% CPU: it boots in ~100 s on a quiet node but past 340 s when starved, and the startup probe allowed 310 s, so a healthy image was killed for being scheduled badly. `backend.startupProbe.failureThreshold` is now a value, defaulting to 60 (ten minutes). Earlier: Region names fixed, see D39 — the first geocoding release labelled every region after its *country* ("Canada" for a whole trip). Cause: the Apple Maps **Server** API nests locality/subLocality/administrativeArea inside `structuredAddress`, while MapKit JS has them flat, so only `country` ever deserialised — a silent failure, since the country parses fine. `ApplePlace` now reads both shapes and the label prefers town → area of interest ("Banff National Park") → district → "State, Country" → country. V39 adds `geocode_cache.resolver_version`; rows below `LABEL_VERSION` are stale and re-resolve as people browse, so the already-cached country names heal themselves without a cleanup script. New `AppleMapsGeocodeResponseTest` pins the wire shape against real JSON. Earlier: Server-side reverse geocoding shipped, see D38 — region headings now show real place names ("Frankfurt") instead of coordinates. New `GET /api/geocode/reverse?loc=…&lang=…` (permitAll, batched, cacheable) answers from four layers before it ever calls Apple: an in-memory map, the exact `geocode_cache` row (V38, point snapped to 10^-4°), the nearest cached point inside `reuse-radius-meters` (300 m), then the Apple Maps Server API — single-flighted, capped at `max-lookups-per-minute`, per-IP capped in the controller, and never blocking the response for longer than its 4 s budget. `AppleMapsTokenService.serverToken()` mints an origin-less JWT for the server API (the browser token's `origin` claim makes Apple reject server-to-server calls); `AppleMapsGeocodeClient` exchanges it for an access token and caches that too. `/api/capabilities` gained `maps.geocoding`. Earlier: "By day & region" gallery grouping shipped, see D37 — a checkbox next to the grid-size picker re-shelves the owner gallery into one section per calendar day, each cut into sub-sections of photos taken within 2 km of each other. Frontend-only: `utils/dayRegionGrouping.ts` does complete-linkage agglomerative clustering (grid-pruned candidate pairs, merged shortest-edge-first, a merge accepted only when *every* cross pair is inside the radius) so a cluster's diameter is capped rather than chained; `composables/useRegionNames.ts` reverse-geocodes each cluster centre through MapKit for a place name and falls back to coordinates. The same toggle is on the public share view. Earlier: Gallery thumbnail loading fixed, see D36 — fast scrolling left tiles blank for ~30 s from two independent causes. Server: `ImageServeController` opened the MinIO stream before returning an ETag-bearing `ResponseEntity`, and Spring 304s a matching conditional GET *without* invoking the message converter, so the `ResponseInputStream` leaked its pooled Apache connection every time — pool of 50, 10 s acquisition timeout, retryable, hence the ~30 s, and cumulative until pod restart. Conditional requests are now answered by `WebRequest.checkNotModified` before any storage call (the builders stopped setting `eTag`/`lastModified`, which `checkNotModified` already stamps), `Cache-Control` gained `immutable` since `publicToken` is the version, and `storage.s3.max-connections` (64) / `connection-acquisition-timeout-seconds` (5) make the pool explicit and fail-fast. Client: new `LazyImage.vue` withholds `src` until the scroll goes idle or the tile has dwelled 350 ms in the band, and shows a shimmer skeleton meanwhile — a 400-tile flick went from 400 requests to 56. Earlier: Per-album map view shipped, see D35 — an album stores where its map opens as MapKit's CoordinateRegion (`map_center_lat/lng` + `map_span_lat/lng`, V37); the owner frames it by dragging the map and pressing "Save this view", and share-link visitors get that framing. No saved view means auto-fit to every pin, as before. `PUT|DELETE /api/albums/{id}/map-view`, owner-scoped; `MapView.of` validates and clamps. Earlier: Map filter shipped, see D34 — a "🗺️ Map" entry in the gallery's tag dropdown swaps the grid for an Apple MapKit JS map with one pin per capture location; clicking a pin (or a cluster) opens a thumbnail strip of the photos taken there, and clicking a thumbnail opens the existing Lightbox. V36 adds `gps_latitude` / `gps_longitude` / `gps_source`; new `GpsExtractor` (worker) reads the EXIF GPS IFD for images and `FfmpegService.extractVideoLocation` reads `com.apple.quicktime.location.ISO6709` for videos, both wired into `processFile` as step 5. `JobType.EXTRACT_GPS` + `FileProcessingService.reextractGps` + `POST /api/admin/extract-gps` backfill existing rows metadata-only; retention-purged rows are excluded because coordinates live only in the original. Server-side `AppleMapsTokenService` mints the ES256 JWT MapKit requires from `maps.apple.*` (helm `appleMaps.*`, .p8 body in the existing Secret) and serves it from the permitAll `GET /api/maps/token`, so public share links get the map too; `/api/capabilities` gained `maps.enabled` so the UI hides the filter when no key is configured. **Live on prod as of 2026-08-21**: `appleMaps.enabled=true`, the key is in the Secret, and the `EXTRACT_GPS` backfill has been run. Layout: `.album-gallery` gains a `map-mode` class that drops its `min-height: 100vh` and bottom padding — the map already sizes itself to the viewport in `PhotoMap.measureHeight`, so leaving both in place reserved the space twice and produced a permanent scrollbar plus a dead band under the map. Earlier: Presentation image groups shipped, see D33 — per `(album, tag)` labelled sections in presentation mode. V35 adds `presentation_groups` (album + tag + `start_file_id` anchor, required `label`, optional `body_text`); `PresentationGroupService`/`Controller` expose `GET|POST /api/albums/{id}/presentation-groups`, `PUT|DELETE /api/presentation-groups/{id}`, plus a read-only `GET /api/albums/public/{token}/presentation-groups` that rides the existing `/api/albums/public/**` permitAll matcher. Frontend: `usePresentationGroups.ts` derives sections by walking the tag-filtered list in `display_order`; `GalleryView` renders them with inline create/edit/delete when logged in, `PublicGalleryView` read-only. The `Lightbox` carries the group into zoomed view as a top-left chapter marker (label + `n / total` + collapsible text, click to dismiss until the next group), since that is where most viewing actually happens. Slideshow record/playback untouched. Earlier: EXIF sort order fixed — photos and videos were stored on two different clocks, see D32. `CaptureDateExtractor` (new, worker profile) resolves an image's EXIF `DateTimeOriginal` against `OffsetTimeOriginal` or `capture-date.fallback-zone`; `FfmpegService.extractVideoCreationDate` now probes all format tags, prefers `com.apple.quicktime.creationdate` over the mvhd `creation_time`, and parses the colon-less `+0200` offset that `Instant.parse` rejects. V34 adds `exif_date_source`; `JobType.EXTRACT_CAPTURE_DATE` + `FileProcessingService.reextractCaptureDate` + `POST /api/admin/reextract-capture-dates` backfill existing rows metadata-only (no transcode, no thumbnail regen). Retention-purged rows can't be fixed — the capture time lives only in the original — so the sweep query excludes them. Not yet run against prod. Earlier: iOS `ShareExtension` and standalone macOS `UploadExtension/` both migrated to TUS — their `Shared/UploadService.swift` files now do `POST /files/` + `PATCH {Location}` with `Upload-Metadata` carrying `filename`/`filetype`/`albumId`/`auth`; legacy multipart code paths removed (also dropped `AppConfiguration.uploadEndpoint` on iOS). `contentId` is intentionally omitted on both so re-shares create new assets, matching previous `/api/upload` behaviour. Two of four blockers for retiring `/api/upload` now closed; remaining: web multipart fallback in `useUpload.ts`, and the main iOS app's `Settings.useTus` toggle (legacy `Uploader.swift` still chosen when `useTus=false`). Earlier: Backend PVC + underlying Longhorn PV retired — `backend.persistence.enabled=false`, helm upgrade dropped the PVC, explicit `kubectl delete pv` + `delete volume` chained the cleanup through Longhorn since reclaimPolicy was Retain. 30 GiB freed; closes Gap 8 follow-ups. Earlier: Phase 4.5 closed — `JobType.REGEN_THUMBNAILS` + `FileProcessingService.regenerateThumbnails` + `POST /api/admin/regen-missing-thumbnails` shipped, mirroring the ROTATE_LEFT pattern; the two video-path admin methods are deliberately not rebuilt because deterministic S3 keys make path-drift impossible in the new architecture. Earlier today: retention is now live-deleting: 2026-05-02 nightly dry-run logged eligible=2408 / TUS=0 / 0 failures cleanly, so `retention.dryRun` flipped to `false` in `values.yaml`. Same commit added a third pass — `RetentionService.runOriginalsOrphanCleanup` — that set-diffs `originals/` listing against `FileMetadataRepository.findAllOriginalsKeys` and deletes keys older than `retention.orphan-grace-hours` (default 24) with no live row. Wired into `RetentionRunner`; `RETENTION_ORPHAN_GRACE_HOURS` env added to `cronjob-retention.yaml`. Closes the deferred TUS post-finish-crash recovery follow-up. Also reverted the rotate-after-purge prohibition: `FileProcessingService.rotateAndReprocess` now falls back to the largest available derivative (output is LARGE=2400px-bounded regardless of source resolution — pixel-equivalent to post-purge browsing), so `rotateImageLeft` no longer 410s on null `filePath` and `GalleryItem.vue` no longer hides the button. Purged assets stay purged: rotation skips the writeback to `originals/` when the original is gone. Earlier 2026-05-01: Phase 6 / Gap 4-finish landed in code: V33 makes `file_path` nullable, `RetentionService` + `RetentionRunner` (one-shot CommandLineRunner under the new `Profiles.RETENTION` profile) sweep DONE rows older than `retention.original-days` and DELETE the S3 original. Helm `cronjob-retention.yaml` runs nightly at 03:17 with `concurrencyPolicy: Forbid`. File-serve falls back to the largest derivative when the original is purged; explicit `?size=original` returns 410 via the new `ResourceGoneException`. Rotate is blocked at the API (also 410) and the rotate button is hidden in `GalleryItem.vue` via the new `originalAvailable` flag on `FileInfo`. Earlier 2026-04-30: Phase 4e R3 cleanup landed; legacy `@Async`/`INGESTED`/`jobs.dispatcher.enabled` and the deferred admin methods were removed.)

---

## Gap inventory (priority order, agreed with user)

1. Persistent jobs table with leases (replace in-memory `ThreadPoolTaskExecutor` queue).
2. Split worker into its own Deployment (currently co-located with upload pod).
3. TUS resumable uploads (currently single multipart POST).
4. Retention CronJob + storage alert (originals kept indefinitely).
5. libvips for image thumbnails (currently ImageMagick).
6. Explicit processing status field on `FileMetadata` (PENDING/PROCESSING/DONE/FAILED).
7. Prometheus queue-depth gauge.

---

## Critical path & sequencing

```
Phase 0  ─ Gap 6 (status field, additive migration, no contract change)
            Gap 5 (libvips swap, internal only)
            Gap 4-prep (PVC alert + retention CronJob skeleton)
                                │
                                ▼
Phase 1  ─ Gap 1 (jobs table + lease-based dispatcher, still in same JVM)
                                │
                                ▼
Phase 2  ─ Gap 7 (Prometheus gauges over the new jobs table)
                                │
                                ▼
Phase 3  ─ Gap 2a (introduce MinIO; both reads + writes from upload pod)
                                │
                                ▼
Phase 4  ─ Gap 2b (split worker into separate Deployment, sharing only DB + MinIO)
                                │
                                ▼
Phase 5  ─ Gap 3 (TUS resumable uploads via tusd Deployment with S3 backend)
                                │
                                ▼
Phase 6  ─ Gap 4-finish (retention CronJob now sweeps MinIO + DB consistently)
```

### Parallelisable
- Gap 6 and Gap 5 are independent; ship in any order in Phase 0.
- Gap 7 metric scaffolding can be drafted alongside Gap 1.
- Gap 4's PVC alert is independent of everything; ship anytime.

### Why this order
1. Status field (6) is additive and removes ambiguity for every downstream phase.
2. libvips (5) reduces memory pressure *before* we add load with the new dispatcher.
3. Jobs table (1) replaces the in-memory queue but stays in the same process — isolates "introduce a queue" risk from "split deployments" risk.
4. Object storage (2a) before splitting (2b): both pods must agree on the storage boundary first.
5. TUS (3) last because it requires the upload pod to be cheap (only stream → MinIO → write job row); that is only true after the worker split.

---

## Decision points (need sign-off before coding)

| #   | Decision                                       | Recommended                                                                                                          | Status |
| --- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------ |
| D1  | Status enum values                             | `INGESTED / QUEUED / PROCESSING / DONE / FAILED / DEAD_LETTER` — richer set keeps reprocessing diagnosable.          | accepted |
| D2  | Backfill strategy for existing rows            | V30: rows with any derivative → `DONE`; else `FAILED`. V31: re-enqueue every FAILED row as QUEUED.                   | accepted |
| D3  | Jobs table key relationship                    | FK `asset_id → file_metadata.id`. Single-purpose queue, not infra.                                                   | accepted |
| D4  | Job granularity                                | One row per asset, fixed pipeline. No sub-step rows until reprocessing demands them.                                 | accepted |
| D5  | Lease duration                                 | 15 min default, configurable via `jobs.lease.seconds`.                                                               | accepted |
| D6  | Poll interval                                  | 2 s, configurable via `jobs.poll.interval-ms`.                                                                       | accepted |
| D7  | TUS implementation                             | **tusd Deployment** with S3 backend writing directly to MinIO. Avoids JVM heap; tusd's state machine is mature.      | accepted |
| D8  | Object storage                                 | **MinIO**, single replica on Longhorn PVC.                                                                           | open   |
| D9  | MinIO deploy shape                             | Hand-rolled Deployment (single Pod, single PVC). Official chart is over-engineered for one node.                     | open   |
| D10 | Existing-data migration to MinIO               | Lazy + one-shot CommandLineRunner (idempotent: skip if HEAD succeeds). No maintenance window.                        | open   |
| D11 | iOS handling of HTTP 202                       | Treat 202 like 200 for "don't re-upload" bookkeeping; surface "Processing…" status in gallery UI optionally.         | accepted |
| D12 | TUS rollout to iOS                             | Coexistence: `/api/upload` stays for old clients; new clients prefer TUS based on `GET /api/capabilities`.           | accepted |
| D13 | Worker concurrency                             | `Semaphore(1)` per worker pod. Add replicas if needed; do not oversubscribe ImageMagick/ffmpeg threads.              | accepted |
| D14 | libvips refactor shape                         | Split into `VipsThumbnailService` (images), `HeicConversionService` (HEIC only), `FfmpegService` (video).            | open   |
| D15 | Dead-letter behaviour                          | Move to `DEAD_LETTER` after `max_attempts=3`, keep original blob. `GET /api/admin/dead-letter` exposes them; UI deferred. | accepted |
| D16 | Circuit-breaker library (Phase 4)              | Resilience4j with `resilience4j-spring-boot3`. Hand-rolled volatile-flag reinvents half of it; we already have actuator + Micrometer.  | accepted |
| D17 | Rotate / admin-thumbs fate after split         | Leave non-functional for one release; admin endpoints throw 503 on api profile until a Phase 4.5 worker-job rewrite. Rare ops cost.   | accepted |
| D18 | Audio reencode locality                        | Stays on api pod (`AudioReencodingService` not `@Profile`-gated). Audio is short and infrequent; no `processing_jobs` row exists.     | accepted |
| D19 | Worker HTTP listener                           | `server.port=-1` in `application-worker.yml`; only management:8081 active. Keep main port off entirely.                              | accepted |
| D20 | Controller profile-gating shape                | Class-level `@Profile("api")` on each controller; grep-able. Avoid `@ComponentScan` excludes.                                          | accepted |
| D21 | Legacy `@Async` cleanup release                | Bundle the removal in R3 (post-soak), not R2. Keeps R2 diff focused on deploy-shape change.                                            | accepted |
| D22 | Worker default replicas                        | 1. Each pod has `Semaphore(1)`, so scale-out gives parallelism but isn't required day-1 on the single-node Pi cluster.                | accepted |
| D23 | Per-job-type discriminator                     | Add `processing_jobs.job_type` (V32) with `JobType.{PROCESS,ROTATE_LEFT}` so admin operations reuse the lease/retry/dead-letter machinery instead of building a parallel queue. Revisits **D4** intentionally — the "single-purpose queue" stance held while there was only one job type. | accepted |
| D24 | tusd S3 key namespace                          | tusd writes to `tus-uploads/{uuid}`; `post-finish` hook S3-COPYs to `originals/{stored_filename}` then DELETEs the tusd object. COPY is server-side in MinIO; bytes never leave MinIO. | accepted |
| D25 | Why COPY rather than heterogeneous keys        | Keeps the `originals/%` invariant that retention (Phase 6), `derivatives/{assetId}/...` paths, and the legacy multipart path all already depend on. One scheme, one sweep rule. | accepted |
| D26 | Auth on TUS endpoint                           | Bearer token in `Upload-Metadata` (key `auth`), validated in `pre-create`. iOS background URLSession can't carry cookies reliably, so the token is the only sane channel. | accepted |
| D27 | Hook-channel authenticity                      | **Amended from the original HMAC sketch** — stock tusd cannot sign hook bodies, only forward client headers, so HMAC was infeasible without a custom shim. Substitute: shared secret embedded in the tusd→api hooks URL path: `http://photo-upload-backend:8080/api/tus/hooks/$(TUS_HOOK_SECRET)`. Controller validates the path-secret with constant-time compare. Cluster-internal only; not exposed via Ingress. Functionally equivalent defence-in-depth against accidental cross-namespace calls. | accepted |
| D28 | Backpressure on TUS                            | Reject in `pre-create` when `JobQueueDepthService.depth > jobs.backpressure.queue-depth-threshold`. Returns 503 → tusd surfaces 5xx → client backs off. tusd never starts buffering bytes for a doomed upload. | accepted |
| D29 | tusd replicas                                  | 1 (single-node Pi). tusd is stateless once `info.json` is in S3.                                                                                                                                                                                                                            | accepted |
| D30 | Duplicate detection in pre-create              | `Upload-Metadata.contentId` (sha256 from client) checked against `file_metadata` exactly like the multipart path. 200 if novel, 409 if dupe. Avoids spending S3 storage on dupe uploads.                                                                                                    | accepted |
| D31 | Existing `UploadBackpressureFilter` fate       | Stays in place for `/api/upload` legacy. TUS gets its own `pre-create` check (D28). Two paths, two filters, same source of truth (`JobQueueDepthService`).                                                                                                                                  | accepted |
| D32 | Capture-date semantics: one clock for photos and videos | Photos and videos were stored on different clocks. metadata-extractor's single-arg `getDate(TAG_DATETIME_ORIGINAL)` hardcodes GMT for zone-less EXIF and ignores `OffsetTimeOriginal` (0x9011), so a photo taken at 14:23+02:00 was stored as 14:23Z — later than it happened — while QuickTime always yielded a true instant. "Reorder by EXIF" compared the two directly and sheared videos against photos by the capture zone's UTC offset. Decision: `exif_date_time_original` holds a **true instant** for every media type. `CaptureDateExtractor` reads `OffsetTimeOriginal` when present and falls back to `capture-date.fallback-zone` (default `Europe/Berlin`, `CAPTURE_DATE_FALLBACK_ZONE`); `FfmpegService` prefers `com.apple.quicktime.creationdate` and tolerates its colon-less `+0200` offset, which `Instant.parse` rejects. New `exif_date_source` column (V34) records the tag used, so legacy rows are findable and the `EXTRACT_CAPTURE_DATE` backfill converges. | accepted |
| D33 | Presentation image groups are anchors, not membership lists | A group is a section marker on one image (`presentation_groups.start_file_id`), scoped to one `(album_id, tag_id)` pair; rendering walks the tag-filtered file list in `display_order` and hands every image to the last anchor it passed. Rejected alternative: explicit per-image group membership — that needs its own ordering column, a bulk-assign UI, and a reconciliation story every time images are reordered or retagged. With anchors, reordering reshuffles sections for free, and a group whose anchor is deleted (FK `ON DELETE CASCADE`), untagged, or filtered out simply stops rendering — no orphan state to clean up. Scope is per-album because a file belongs to exactly one album, so a user-global per-tag group would have no consistent image set. Label is required, text optional; `body_text` is `VARCHAR(4000)` not `TEXT` because `ddl-auto: validate` rejects LONGVARCHAR against a plain `String` field. Groups deliberately do **not** affect slideshow record/playback — those keep walking the flat list. The grid heading alone was not enough: most viewing happens zoomed in, so `Lightbox` also takes a `groupContext` prop (`{label, text, position, total}`, derived by `groupContextFor`) and renders a top-left chapter marker — suppressed while recording (same corner as the REC badge) and while playback controls are hidden. Images ahead of the first group resolve to `null`, not an empty hint. Clicking the marker dismisses it; it returns when the reader crosses into a different group (the watcher keys on the group **id**, not the label, so two adjacent groups sharing a label still re-trigger it) or when the lightbox closes, since `groupContext` goes null there. | accepted |
| D34 | Map is a view in the tag dropdown, not a tag | The map ships as a sentinel option (`__map__`) in the existing "Filter by tag" `<select>`, proxied through a `filterSelection` computed that sets a separate `mapMode` ref and clears `selectedTag`. It is deliberately **not** written into `selectedTag`: that value feeds audio recordings, presentation groups and analytics filter events, and a sentinel leaking into any of those would be a silent data bug that only shows up in the analytics table weeks later. One dropdown still means one active filter, so entering map mode clears the tag rather than intersecting with it — an invisible tag filter silently hiding pins is worse than showing the whole album. Storage: coordinates are their own columns (V36) rather than a derived-on-read EXIF parse, because the original is deleted by retention and a read-time parse would lose the location for every purged asset. `gps_source` mirrors `exif_date_source` so the `EXTRACT_GPS` sweep converges — without it every location-less asset would be re-read on every pass. Provider: Apple MapKit JS, chosen over Leaflet/OSM because the install already targets Apple clients; the cost is that the map is dark unless an Apple Developer key is configured, which `/api/capabilities.maps.enabled` makes explicit instead of leaving a grey box. The token endpoint is unauthenticated on purpose (public share links show the map), which is safe because the token is short-lived, grants nothing but map tiles, and is origin-pinned via `maps.apple.origin`. | accepted |
| D35 | Album map view is a saved region, set by the map itself | Each album stores where its map opens: `map_center_lat/lng` + `map_span_lat/lng` (V37), which is MapKit's `CoordinateRegion` verbatim. Not a zoom level, because MapKit JS has no integer zoom — `map.region` is the only thing readable after a pan and the only thing writable to restore one, so storing it raw means no conversion can drift. The span *is* the zoom. All four columns move together; any NULL means "no saved view" and the map falls back to `showItems` framing every pin. UI: no coordinate entry. The owner drags and zooms the map to the framing they want and presses "Save this view" on an overlay button, which reads `map.region` off the live map — the map is a better input widget for a map view than four number fields, and it cannot produce a framing the owner has not already looked at. "Reset" clears it. The auto-fit default stays for albums with no saved view (an album can span one park or three continents), but it is only ever a guess at what the album is *about*: a Toronto trip with three airport-layover photos fits to include the layover and opens showing mostly water. A saved view therefore overrides auto-fit outright rather than being merged with the pin bounds. Validation lives in `MapView.of`, which returns null — not an exception — for anything unusable, because "clear the view" and "you sent nonsense" want the same outcome; a persisted zero span would be a region no pan could escape. Oversized spans clamp instead of failing, since a fully zoomed-out map legitimately reports a span at the limit. Endpoints are `PUT|DELETE /api/albums/{id}/map-view`, owner-scoped, kept off `PUT /api/albums/{id}` because that one is name-and-description and round-tripping a name through it would trip the duplicate-name check. Share-link visitors receive the saved view (it is how the owner framed the album, and it reveals nothing the pins do not) but cannot change it. | accepted |
| D36 | Thumbnail serving: answer conditional GETs before opening the object stream, and gate the browser's lazy loads on scroll | Two independent causes of "scroll fast, tiles stay blank for ~30 s, then fill in". Server: `ImageServeController` opened the MinIO stream and *then* returned a `ResponseEntity` carrying an ETag. Spring's `HttpEntityMethodProcessor` short-circuits a matching conditional GET to 304 and returns **without invoking the message converter**, so the `ResponseInputStream` was never closed and its pooled Apache connection never returned. The SDK pool defaults to 50 with a 10 s acquisition timeout, and pool timeouts are retryable — three attempts is the ~30 s the user saw, and the leak is cumulative until the pod restarts. A browser reload revalidates every cached thumbnail at once, which is exactly the burst that drains it. Fix: evaluate `If-None-Match`/`If-Modified-Since` via `WebRequest.checkNotModified` *before* any storage call, so a 304 costs one DB read and zero MinIO round trips. The builders no longer set `eTag`/`lastModified` themselves — `checkNotModified` stamps both either way, and setting them twice emits duplicate headers. `Cache-Control` also gained `immutable`, which is honest here because rotation and reprocess regenerate `publicToken` (the URL *is* the version), and it stops reload from revalidating at all. Pool size and acquisition timeout are now explicit (`storage.s3.max-connections`, default 64 — above `server.tomcat.threads.max` so the servlet container stays the binding constraint — and `connection-acquisition-timeout-seconds`, default 5, so an exhausted pool errors visibly instead of stalling). Client: `loading="lazy"` only defers the *start* of a fetch and the browser never cancels one because the image scrolled past, so flicking through a large album enqueued a GET per tile in DOM order and whatever landed on screen queued behind all of them. New `LazyImage.vue` withholds `src` until either the scroll goes idle (120 ms) or the tile has dwelled in the band for 350 ms — fast scrolling requests nothing, slow scrolling still preloads. Measured on a 400-tile harness: a flick to the bottom issued 400 requests before, 56 after. Rejected: virtualising the grid (drag-reorder, shift-click ranges and the map filter all index into the rendered list), and redirecting to presigned MinIO URLs (exposes the bucket and defeats the immutable cache). | accepted |
| D37 | "By day & region" gallery grouping, clustered with complete-linkage rather than a running-blob pass | The gallery could already be filtered by tag or read on a map, but neither answers "what did we do on the Tuesday, and where". A checkbox (off on every visit and deliberately not persisted — the plain grid is what the gallery is, and a setting from an earlier session silently re-shelving it surprises more than one click costs; kept out of the tag dropdown because it is orthogonal to the filter — it re-shelves whatever the filter already selected) renders day sections, each split into place clusters. Clustering lives in `utils/dayRegionGrouping.ts` and is deliberately not the obvious "start a group, keep adding while the next photo is near the previous one" pass: single-linkage chains, so a walk across a city collapses into one 20 km "region" whose ends are nowhere near each other. Instead: project the day's points to local metres, bucket them into a grid of radius-wide cells so only the 3×3 neighbourhood yields candidate pairs, sort those pairs ascending, and merge greedily under the complete-linkage rule — two clusters join only when *every* cross pair is within 2 km, so a finished region is guaranteed at most 2 km across. Above 1500 located photos in one day (bulk imports stamped with a single date) it falls back to a leader pass with a half-radius acceptance, which keeps the same diameter guarantee for a cruder choice of groups. Days, clusters and photos all keep album order — grouping re-shelves the gallery, it does not re-sort it; photos without GPS get a trailing bucket per day, photos without a date a trailing day. Region headings are reverse-geocoded via `mapkit.Geocoder` (`composables/useRegionNames.ts`, module-scoped cache keyed to ~100 m, one lookup per centre) and read as coordinates until a name lands or when Apple Maps is not configured — decoration, never data. Drag-reorder is off in this view (the rendered order is derived, so dropping into it would mean nothing) and shift-select resolves its range through an id→album-index map. Offered in the owner gallery and on public share links; not in the owner's presentation mode, which is a tag-driven narrative with its own group headers. **Requires a tag filter**: the toggle is hidden until one is chosen and groups exactly what that tag selected. Day sections over an entire album are as long as the album and say nothing — the grouping is a second reading of a chosen set, not a way to browse everything, and on the share view that also stops it standing in for the "pick a tag" prompt. Suppressed in map mode and during recorded playback, where the slideshow drives the order. | accepted |
| D38 | Reverse geocoding server-side behind a shared cache, not in the browser with MapKit's geocoder | D37 shipped region headings as coordinates. The obvious fix — `mapkit.Geocoder` in the page — is the wrong one three times over: it drags the whole MapKit bundle into a page that may never draw a map, its cache is per-visitor so the same album re-asks Apple for every person who opens the share link, and the quota is spent on questions we have already answered. A place name for a coordinate is the most cacheable fact in the app, so the cache belongs on the server, once, for everyone. `GET /api/geocode/reverse` takes up to `max-points-per-request` (60) `loc` params so one album view is one round trip, and answers `Cache-Control: public, max-age=86400` when every point resolved, `no-store` when any did not (freezing a merely-slow name for a day would be worse than re-asking). `ReverseGeocodeService` stops at the first layer that answers: in-memory map → exact `geocode_cache` row → nearest cached point inside `reuse-radius-meters` (300 m; a photo region is 2 km across, so one row can name a whole village) → Apple. Points are snapped to 10^-4° (~11 m) so the key is an integer pair and duplicate asks collapse; a null `place_name` is a real negative entry, re-checked after `negative-ttl-days`, and a *neighbouring* negative row is never reused (the sea 300 m away says nothing about the village here). Concurrent asks for one point share a single in-flight future, so twenty simultaneous visitors cost one call. Lookups that outlive the 4 s batch budget keep running and land in the cache for the next visitor rather than holding the response. V38 adds `geocode_cache` (lat_e4, lng_e4, language unique). Apple's Server API needs its own token: `serverToken()` mints the same ES256 JWT *without* the `origin` claim, because a server-to-server call sends no Origin header and Apple 401s the browser token; `AppleMapsGeocodeClient` exchanges it at `/v1/token` and caches the access token to expiry. Being unauthenticated (public share links need it) it is fenced on three sides — points per request, per-IP requests per minute, and a pod-wide ceiling on calls that actually reach Apple — and every fence degrades to "no name", so the UI falls back to coordinates instead of breaking. Metrics: `geocode_cache_total{layer}` and `geocode_lookup_total{result}`. Frontend `useRegionNames.ts` batches misses with a 50 ms window, backs off 60 s on 429 or error, and skips the endpoint entirely when `maps.geocoding` is false. **Boot 4 note:** the client builds its own `RestClient.builder()` — injecting the auto-configured `RestClient.Builder` bean failed the api pod's boot, because Boot 4 moved that auto-configuration into the `spring-boot-restclient` starter this app does not depend on. `ApiProfileContextTest` should have caught it and could not: with `webEnvironment = NONE` its context never started at all (SecurityConfig's chain wants a `CorsConfigurationSource`, which only exists with the WebMvc infrastructure loaded), so it reported a CORS error instead of checking beans. Switched to `MOCK`, and it now asserts the three geocoding beans on the api graph and their absence on the worker's. `ProcessingJobLeaseTest` still has the same `NONE` problem and is untouched. | accepted |
| D39 | Read Apple's `structuredAddress`, label by town, and version the cache so wrong names expire themselves | D38 shipped with the response modelled on MapKit JS's flat `Place`. The Maps Server API puts the town, district and state inside `structuredAddress` and only `country` at the top level, so exactly one field matched and every region in a country was labelled after the country. The failure mode is the instructive part: it is invisible to a compiler, to a mock-based test (the mock returns whatever shape the mock was built with) and to a smoke test that only checks a name came back — it needs a test against real JSON, which `AppleMapsGeocodeResponseTest` now is. `ApplePlace` reads structuredAddress first and the flat fields as a fallback, so both shapes work. Label rule: town (what people recognise) → area of interest (a landscape photo sits in no town, and "Banff National Park" beats "Alberta") → district → "Alberta, Canada" → country. District is deliberately *not* preferred over town: a region is up to 2 km across, so naming it after whichever neighbourhood the centroid landed in is more precise than it is true. The cache made this worse than a normal bug — the wrong names were already stored and nothing would re-ask — so V39 adds `resolver_version` and rows below `LABEL_VERSION` (now 2) are treated as stale, re-resolved as people browse and overwritten in place; stale neighbours are also barred from the reuse radius so old labels cannot spread sideways. Chosen over a TRUNCATE because it also handles the next rule change, and over a background re-sweep because spreading the re-asks across normal browsing keeps the Apple quota flat. | accepted |
| D40 | Startup probe budget is a ceiling for the worst node, not a target | The api pod entered a restart loop after a deploy — exit 137, `Container api failed startup probe`, no application error and 210 Mi of a 1 Gi limit in use. Both backend pods had landed on `k8s-node23`, which was at 96% CPU; Spring Boot's startup is CPU-bound and the api requests only 100m, so boot stretched from the ~100 s it takes on a quiet node to past the 310 s the probe allowed (10 s delay + 30 × 10 s). Nothing was wrong with the image, and restarting it made things worse: each restart re-ran the boot on the same busy node. `failureThreshold` is now `backend.startupProbe.failureThreshold`, default 60 (ten minutes). The 100m CPU request is left alone deliberately — it was lowered on purpose in R2 slim once the encode pipeline moved to the worker, and raising it to fix a boot-time symptom would reserve a permanent core-share for work that only happens for 100 seconds. If the loop returns on a quiet node, that is a real regression and the budget is not the answer. | accepted |
| D41 | Cut "by day" on the capture wall clock, recovered from a stored UTC offset — never on the viewer's timezone | `exif_date_time_original` is a true instant (D32), which is what album sort order needs and exactly the wrong thing to read a calendar day out of: `dayKey` used `Date.getFullYear/Month/Date`, so a Toronto album viewed from Frankfurt had every photo after 18:00 local shelved under the next morning — the reported "midnight border a couple of hours off", six hours in summer. The day a photo belongs to is the day its own camera saw, so what was missing is the offset that instant was resolved against. V40 adds `capture_utc_offset_seconds`; `CaptureDate` carries it and every extractor that produces an instant also produces it — `OffsetTimeOriginal` when the phone wrote one, the `capture-date.fallback-zone` offset *at that instant* (DST included) when it did not, and the offset on `com.apple.quicktime.creationdate` for video. The fallback case is the subtle one and it needs no configuration: undoing the offset that was applied recovers the camera's original wall clock whether or not the fallback zone was the right guess, because that is the same arithmetic run backwards. So no per-album "trip timezone" setting exists and none is wanted — a manual setting could only ever restate what the file already says, and would be wrong for an album spanning two zones, which per-file offsets handle for free. Not stored: `MVHD_UTC` videos (the atom is UTC and keeps no local clock) and rows whose original retention already purged. Those fall back to the album's dominant offset — one trip is normally one zone — and only then to the viewer's timezone, which is the old behaviour and still right for photos taken at home. The `EXTRACT_CAPTURE_DATE` sweep now also selects dated rows with no offset, restricted to the three sources that always yield one so it stays self-shrinking; `POST /api/admin/reextract-capture-dates` backfills, and rows it cannot fix are exactly the two fallback cases above. Following the offset through `AlbumService.duplicateAlbum` turned up an older bug in the same copy block: it never carried `gps_latitude`/`gps_longitude`/`gps_source`, and left the entity's `QUEUED` default in place. A duplicate shares the original's bytes but re-derives nothing and has no job enqueued, so its assets showed as "still processing" for good and — because every admin sweep filters on `DONE` — could never be backfilled either. Both now copied, pinned by `AlbumServiceTest.duplicateAlbumCarriesCaptureMetadataAndProcessingState`. | accepted |
| D42 | The six long-standing test failures were stale tests, not stale code — fixed by correcting the tests | The suite had carried six red tests long enough that CLAUDE.md listed one as a baseline to ignore, which is how a suite stops being a signal. All six were assertions about *how* the code works that stopped being true when the code got better. Four in `JobLeaseServiceTest` and one in `TagServiceTest` verified an explicit `repository.save(...)`; both services now mutate an entity loaded inside their own `@Transactional` method and let the flush write it, which is correct JPA and untestable by verifying a save that never happens — they assert on the entity instead, which the stubbed `findById` hands back as the same instance. One of those four also still expected `FAILED` on a non-exhausted attempt, but the code deliberately re-queues (`QUEUED`), because `findNextLeaseableId` only selects QUEUED rows and a job parked in FAILED is never retried; it is renamed `markFailedBeforeMaxAttemptsRequeuesForRetry`. The sixth, `FileProcessingServiceStatusTest.successfulProcessingTransitionsToDoneAndIncrementsAttempts`, stubbed `generateAllThumbnails` as `new Path[3]` — three nulls, which is that method's "every size failed" signal. `processFile` grew a guard that throws on it rather than marking an asset DONE with no derivatives, so the "successful" run was correctly failing; the stub now returns three paths. No production code changed for any of the six. | accepted |
| D43 | Video size ceiling: the cap is not the bug, uploading originals is | **Partly resolved 2026-08-23 — cap raised to 2 GiB, refusal made legible, no client re-encoding.** An ordinary 4 min 30 s iPhone clip (4K, roughly 1.6 GB) is refused at `POST /files/` with `413 ERR_MAX_SIZE_EXCEEDED`. The reflex is to raise `tus.maxSize`; the data says otherwise. Across the whole library there are 85 videos totalling 4.18 GB, mean 50 MB, and the **largest ever stored is 137 MB** — nothing has ever come within 3x of the 500 MB cap, because everything larger has been silently refused. The Toronto trip (2026-08-09 to 08-15) is the clearest example: 397 photos at 1964 MB but only **4 videos at 193 MB**, the biggest 69 MB. Those are not the trip's videos, they are the ones small enough to fit. So this reads as a storage-efficiency question and is actually **data loss**: real footage is not backed up anywhere but the phone. Chain of causes, in the order they bite. (1) The iOS client uploads the **raw original**: `Uploader.exportAsset` calls `PHAssetResourceManager.writeData` on the `PHAssetResource`, a byte-for-byte copy, and `AVAssetExportSession` appears nowhere in the app — nothing downscales before the wire. (2) tusd is the **only** gate: launched with `-max-size=524288000`, while `TusHookService.preCreate` checks auth, queue depth and `contentId` but never size, Traefik (not nginx — k3s default) imposes no body limit, and the ingress carries no middleware. One knob, `tus.maxSize`, feeds both tusd and `TusProperties`. (3) The server-side transcode does **not resize**: `FfmpegService.transcodeVideo` runs `libx264 -profile:v main -level 4.0 -pix_fmt yuv420p -preset medium -c:a aac -b:a 128k -movflags +faststart` with no `-vf scale` — only the poster frame is scaled, at `scale=600:-1`. A 4K HEVC source therefore comes back 4K H.264, a less efficient codec at equal resolution, so the "web-playable" derivative can be **larger than the original it was made from**, and `-preset medium` at 4K on this cluster is slower than real time. (4) Retention makes that derivative load-bearing: 50 of 85 videos have a transcode but only 28 still have an original, so for most videos **the transcode is the backup** — quality and size of that copy are archival decisions, not convenience ones. (5) The client never resumes: `TusUploader.startPatch` hardcodes `Upload-Offset: 0` and sends the whole file in a single PATCH, so TUS's one real advantage is unused; raising the cap alone would make every dropped connection restart a multi-gigabyte transfer, which is worse than refusing it. Storage context: MinIO is a 30 GiB Longhorn PVC at 13 GiB used / 18 GiB free, on a node with 683 GiB free, so the volume can grow but has not been revisited since it was sized. Recommended order when this is picked up, smallest blast radius first: **(a)** downscale on the phone — `AVAssetExportSession` at 1080p HEVC before upload turns the 1.6 GB clip into roughly 300 MB and fits under the *existing* cap, needing no server change, no redeploy and no bigger disk, and it is the only one of these that fixes the 413 at all, since the rejection happens before a byte is sent; **(b)** add `-vf scale=-2:1080` to the ffmpeg command so the archival copy stops being 4K, which also speeds the encode by roughly the pixel ratio; **(c)** make the client chunk and resume from the server's reported offset; **(d)** only then reconsider `tus.maxSize` and the PVC size. Two traps for whoever implements (a). The local re-upload guard hashes the **uploaded bytes** (`Uploader.sha256(ofFileAt:)` over the exported file, stored via `UploadStore.storeChecksumMapping`), and video encoders are not byte-deterministic, so transcoding then hashing makes the fingerprint change every run and the guard silently stops working — hash the original, or lean on `contentId`, which is `asset.localIdentifier` and therefore unaffected. And downscaling is irreversible for the server copy: the phone keeps its 4K, but for anyone whose phone is lost the 1080p version is what survives, which is a product decision about what "back up your photos and videos" promises, not a technical default to pick quietly. **Decided and shipped 2026-08-23, deliberately not (a).** Client-side re-encoding is ruled out for now — the phone keeps doing a byte-for-byte export — so the two traps above stay unsprung and the server keeps receiving true originals. Instead: **the cap moved from 500 MB to 2 GiB** (2147483648) in `values.yaml`, `application.yml`, `TusProperties` and both Helm templates, which admits the 4:30 clip that motivated this entry; and **the refusal became legible**, because a cap of any size still needs an answer for the file that does not fit. New `UploadSizeLimit` (iOS, pure and tested) decides and phrases it; `TusUploader` checks the exported size against the advertised `tus.maxSize` *before* `POST /files/` and maps a 413 onto the same path, producing "Too big to back up: IMG_4021.MOV is 2.4 GB, the server accepts up to 2 GB" where the log used to read "HTTP 413". A refused asset is recorded in `UploadStore` together with **the limit that refused it** — never as uploaded — so scans stop re-exporting a file that cannot land, and raising the cap later un-skips everything the new cap admits without the user doing anything; the count shows on the Sync tab whenever it is non-zero. The web client got the same pre-flight and the same two-number message. **The blocker this exposed:** Traefik v3.5 fronts the cluster with `respondingTimeouts.readTimeout` at its default **60 s**, and that is the limit on reading an entire request *including the body*. Since `TusUploader.startPatch` still sends the whole file in one PATCH with `Upload-Offset: 0`, an upload lasting over 60 s is cut by the ingress whatever the cap says — 2 GiB inside 60 s would need a sustained 273 Mbit/s, and even 500 MB needs 67 Mbit/s, which is the most likely reason nothing above 137 MB was ever stored. Raising that entryPoint timeout is cluster-level work outside this repo and is the next step; chunked resume (c) is what turns "possible" into "reliable", and the ffmpeg `-vf scale` (b) is now purely an archival-size question rather than a data-loss one. | partial |
| D44 | Scoped upload tokens instead of the account password in `Upload-Metadata` | **Shipped 2026-08-23.** TUS uploads authenticate through `Upload-Metadata` because tusd forwards no arbitrary headers to its hooks (see D26), and that value was the account's own `email:password`. base64 is not encryption, and tusd persists the metadata of every in-progress upload to a `.info` object in the bucket — so the real password was being written to object storage, on a path with a different lifetime and a different backup story from the Keychain. The channel is unchanged; what travels through it is not. V41 adds `upload_tokens`, storing only a hex SHA-256 of a 256-bit random token, so a dump of the table replays nothing. `UploadTokenService.issue` mints `zut_…` and sweeps expired rows on the way through — issuing is the only thing that grows the table, so it is the only moment that needs to shrink it, and that avoids a CronJob whose whole job would be deleting a handful of rows. `POST /api/upload-tokens` returns the plaintext once, plus **`expiresInSeconds`** rather than only the absolute instant: a phone with a skewed clock comparing the server's timestamp against its own idea of now would refresh at the wrong moment, or never. `TusHookService.authenticate` routes on the `zut_` prefix and still accepts legacy `email:password`, which is what makes this deployable in either order — no client is stranded, and the prefix cannot be confused with an e-mail address. Clients: `UploadTokenStore` is in-memory only (a token on disk would be one more copy of a credential, which is the problem this reduces), refreshes 5 minutes before expiry so a token cannot die mid-batch, coalesces concurrent fetches so three parallel uploads mint one token, drops the token on `credentialsDidChange`, and `TusUploader` retries a 401 once with a fresh token rather than reporting a routine credential rotation as a permanent failure. The share extension mints one per share; the web client caches one keyed by the credentials it was minted with, which makes logout self-invalidating without a circular import into `useAuth`. All three fall back to the legacy value when the endpoint is absent. Changing or resetting the password revokes every outstanding token, so "I changed my password" keeps meaning "whatever had my old credentials is locked out". TTL is `tus.uploadTokenTtlHours` (default 24). This is hardening, not a cure: the token is still a bearer credential in the same channel. What changed is the blast radius — it authorises starting an upload, it expires, and discarding it costs the user nothing. | accepted |
| D45 | Renditions a client cannot decode are made on the worker, never inside the request | The AAC sibling introduced for iOS was produced lazily, inside `GET /api/r/{token}/audio?format=m4a`, on the reasoning that it would happen at most once per old recording ever. It does — but that once takes **60 seconds** on this cluster (measured: `13:36:36 → 13:37:36` in the api log), and `AVPlayer` abandons the request long before, so the play that triggered the work was always the play that failed. That alone would have been merely slow. Two other things turned it into the intermittent "does not work on iPhone, works if you wait, fails again next time" the user reported. First, `AVPlayer` opens several ranged connections and drops them constantly; the abandoned write threw out of the `StreamingResponseBody`, `@ExceptionHandler` then tried to render `ErrorResponse` into a response already committed as `audio/mp4` (`HttpMessageNotWritableException: No converter … with preset Content-Type 'audio/mp4'`), and Tomcat's recycled header state came apart — `MimeHeaders.getValue` NPEs and `arraycopy: last source index 17 out of bounds for object array[16]` — poisoning the connection for whatever request came next. Second, `ObjectStorageService.exists` caught its 404 *outside* the circuit breaker, so every "sibling not there" answer counted as a MinIO failure against a 50%-of-20 window; enough of them tripped the breaker OPEN and failed unrelated storage calls for its ten-second wait. Fix: `JobType.TRANSCODE_AUDIO_AAC` + the profile-less `RecordingAudioService` do the transcode on the worker. **First deploy of that fix 500'd, and the reason is worth keeping**: the job row was given the recording id in `asset_id`, whose column is `VARCHAR`-free but not constraint-free — `fk_processing_jobs_asset` references `file_metadata(id)`, so every enqueue died on `Cannot add or update a child row`, once per poll tick. V42 adds a nullable `recording_id` with its own `ON DELETE CASCADE` foreign key into `slideshow_recordings` and makes `asset_id` nullable; a job now carries exactly one of the two, and deleting a recording clears any transcode still queued for it. This is the one job type that is not asset-backed, which `JobDispatcher` special-cases when settling the row. The api only asks `isAacReady` and, when it is not, queues once and throws `AudioNotReadyException` → **503 + `Retry-After: 5`**. iOS polls the new permitAll `GET /api/r/{token}/audio/status` before it hands anything to `AVPlayer` and shows "Preparing this commentary for iPhone", because `AVPlayer` reports every HTTP failure as one undifferentiated decode error — "not made yet" and "broken" are indistinguishable to it, and only one is worth waiting through. A dead-lettered transcode reports `failed` so the client stops polling instead of waiting out a job that will not run again. The streaming body now swallows the client-gone `IOException`, and the generic handler writes nothing into a committed response. Browsers are untouched throughout: they never pass `format`, so they keep getting the Opus master. | accepted |
| D46 | A sweep that deletes by "no DB row" must be told about **every** table that owns bucket keys | Chasing why an old commentary 404'd from MinIO turned up the cause, and it is not a bug in the playback path: `FileStorageService.purgeOrphanedS3Objects` — `POST /api/admin/purge-orphaned-s3` — listed the **whole bucket** and deleted every key absent from `FileMetadataRepository.findAllStoredPaths()`. Recording audio does not live in `file_metadata`; it lives in `slideshow_recordings.audio_path`. So every narration in the bucket read as garbage, and one non-dry run deleted the lot. The rows survived, still pointing at `audio/{uuid}.webm` keys that no longer existed, which is why playback later failed with `NoSuchKeyException` on recordings that had worked for months. **Damage, measured 2026-08-25: 40 of 45 recordings lost their audio** — everything made before 2026-08-17, across New York, Mallorca, Amsterdam, Rom, Iceland and Seoul. Unrecoverable: Longhorn's recurring-backup jobs cover mariadb, couchdb, reposilite, registry and lunchy but **not the MinIO volume**, and its only snapshot (2026-08-20) postdates the deletion. The rows are still there, so the albums still list commentaries that can never play. Fixed three ways. The known-key set now unions `file_metadata` paths, `SlideshowRecordingRepository.findAllAudioPaths()` and the derived `.m4a` sibling keys — derived rather than stored, so they are named here exactly as the serving path names them. `tus-uploads/` is skipped outright and counted separately as `skippedInFlight`: those objects are owned by tusd and have no row *by design* until the post-finish hook moves them, so "no row" there means "in flight", and only `RetentionService`'s grace-period pass can safely judge that prefix. And `FileStorageServiceOrphanPurgeTest` pins all of it, including that a genuinely unowned key is still deleted, because a sweep that deletes nothing is not a fix. The wider lesson is the entry title: this endpoint infers *intent* from an incomplete model of ownership, and every new table that writes to the bucket silently widens the blast radius. The two follow-ups worth doing are backing up the MinIO volume like every other stateful workload, and making the dry-run output the only thing an operator ever reads before a real run. | accepted |
| D47 | A resumable upload that never asks the server for its offset is not resumable — and its retry budget must count *consecutive* stalls | **Shipped 2026-08-26 (iOS client only; no server change).** The TUS client sent one `PATCH` carrying the whole file with `Upload-Offset: 0` hardcoded. Traefik's 60s `readTimeout` kills any request slower than that, so every large video failed and the next attempt started again from byte 0 — the exact failure D43 left open, dressed as a network error. Both uploaders (`TusUploader` in the app, `UploadService` in the share extension) now share `Shared/TusChunking.swift`: 4 MiB chunks, `Upload-Offset` parsed from the response, the next range sliced into a temp file. Three consequences worth writing down. **(a)** The `HEAD` that discovers the server's offset belongs on the *background* `URLSession`, as a download task whose method is HEAD — a `URLSession.shared` request there simply never returns once iOS suspends the app after `urlSessionDidFinishEvents`, and the asset is then stuck in `uploading` with no callback left to clear it. `HEAD` is only the resume path: a freshly created TUS resource is empty, so the first `PATCH` goes straight out at offset 0. **(b)** The resume budget (`TusResumeBudget`, 3) must be refilled every time the offset advances. As a per-file lifetime cap it is a bug: a 2 GiB video is ~500 chunks, three dropped chunks over an hour on a phone is ordinary, and the file would be re-exported from zero for nothing. **(c)** Slicing is file I/O and must not run on the session's delegate queue, which owes callbacks to every other upload in flight. Related, in the same pass: a mixed share batch is a partial success (`ShareUploadOutcome`) rather than a total failure that invited a duplicate-creating retry, and its "already landed" skip list carries over only to a byte-identical retry (`ShareRetryBatch`) — inheriting it across a *partly* overlapping share would report the shared files as uploaded without sending a byte. Still open: the 4 MiB chunk is a bet against a 60s ingress timeout (~0.53 Mbit/s); a worse link stalls, the budget runs out, and the next scan re-exports. |
| D48 | Presentation mode is a *reading* screen on iOS, and the phone reads groups rather than writing them | The web gallery's presentation mode is the creator's own read of an album: one tag, shelved into labelled chapters, with its audio commentary to hand. iOS had the commentary (`NarrationSetupView`) but no presentation, so `presentation_groups` written in the browser were invisible on the device most viewing happens on. Ported as `Views/Presentation/` behind a **Present** entry in the album menu, with three deliberate choices. **Read-only groups**: placing a chapter means judging a run of photos against the whole album with a keyboard to hand, which is a desk job; the phone fetches `GET /api/albums/{id}/presentation-groups` and offers no create, edit or delete, so there is no second write path to keep consistent with the web one. **The shelving is pure and shared in shape, not in code**: `PresentationGallery` (Utils) is a line-by-line port of `usePresentationGroups.buildSections`/`groupContextFor` — same anchor semantics, same dropped leading run when the first photo starts a chapter, same silent skip for an anchor missing from the filtered list — pinned by `PresentationGalleryTests` so the two clients cannot drift. **The chapter marker belongs in the full-screen view**: the grid heading alone is not enough for the same reason it was not enough on the web (D33), so `PresentationPhotoView` is a pager over the filtered list carrying label, `n / total` and the body text, dismissible and re-armed on the group **id** changing. A tag filter is required exactly as on the web — an unfiltered album is as long as the album and says nothing — except for an album with no tags at all, which is presented whole. Recording stays in `NarrationSetupView`; presentation only plays. No server change: both endpoints already existed and neither was iOS-specific. | accepted |
| D49 | The map is native MapKit on iOS, offered wherever the album is read — including presentation, which the web does not do | The web keeps the map out of presentation mode (`mapFilterAvailable = !presentationMode && …`), so an owner reading their own album on the phone had no way to it at all: iOS had no map anywhere. Ported as `Views/Map/` and offered in both places — a third album layout beside Grid and By Day & Place, and an entry in the presentation filter. Four choices worth recording. **`MKMapView`, not SwiftUI's `Map`**: the web leans on MapKit's clustering to merge pins that overlap at the current zoom and to hand the members back on selection, and `clusteringIdentifier` / `MKClusterAnnotation.memberAnnotations` exist only on the UIKit view — SwiftUI's `Map` would draw the pins on top of each other. **No `maps.enabled` gate**: that capability exists because MapKit JS will not draw a tile without a token from `/api/maps/token`, which is a browser problem; native MapKit needs no key, so the only honest gate is whether the album has a located photo, and gating on the capability would hide a working map on an install with no Apple Developer key. **The filter is an enum, not a sentinel**: the web proxies `__map__` through `filterSelection` so it never reaches `selectedTag` (D34); `PresentationFilter` (`.unset` / `.tag(String)` / `.map`) makes the same guarantee structurally — `tagName` is nil for the map, so a sentinel cannot reach the commentary lookup or the chapter markers even by mistake. **Saved framing is read everywhere, written only in the album view**: `Album` gained the four `map*` fields (appended as optional `var`s so existing `Album(...)` calls keep compiling) and the map opens at the owner's framing when there is one and fits every pin when there is not; Save and Reset are offered in the album layout, as on the web, and withheld in presentation, which edits nothing. | accepted |
| D50 | Chapters are writable on the phone after all — the argument for read-only was about the *form*, not the *device* | **D48** kept presentation groups read-only on iOS on the grounds that placing a chapter means judging a run of photos against the whole album with a keyboard to hand. Half of that held and half did not: the judging is easiest exactly where the photos are being read, which after **D49** is the phone as often as the browser. Writing is offered from the two places the judgement is actually made — a long press on the photo a chapter should start at, and a menu on the heading of one that already exists — rather than from a management screen listing chapters out of context, which is the shape that genuinely does need a desk. What stays fixed is what the server fixes: a chapter's tag and anchor are shown in the form and cannot be edited, because `PresentationGroupRequest` ignores both on update and moving a chapter is delete plus create. Validation is duplicated rather than left to the server — `PresentationGroupDraft` is a pure port of `normalizedLabel`/`normalizedText`, pinned by tests — because the web can make an over-long value unreachable with `maxlength` on an `<input>` and SwiftUI's `TextField` has no such thing, so without it the phone's only feedback on a 4001-character body would be a 400. The anchor is now marked in the grid as well as in the heading, which is what makes an empty long-press menu on that photo legible rather than a dead end. No new endpoints: all three have existed since **D33**. | accepted |
| D51 | `SWIFT_APPROACHABLE_CONCURRENCY` stays on the ShareExtension target only — the app is not written for it yet, and the compiler will not say so | The Swift 6 move (`SWIFT_VERSION` 5.0 -> 6.0, `IPHONEOS_DEPLOYMENT_TARGET` 15.0 -> 26.0) left this flag and `SWIFT_UPCOMING_FEATURE_MEMBER_IMPORT_VISIBILITY` set on the ShareExtension target alone, while a comment in `AuthenticatedImageLoader` claimed "this build has `SWIFT_APPROACHABLE_CONCURRENCY` on" to explain why its `tryMap` must be a named `nonisolated static func`. Turning it on for the app to match **compiled cleanly with no warnings, passed all 561 tests, and crashed the running app** with `BUG IN CLIENT OF LIBDISPATCH: Assertion failed` — the very failure the comment describes. Under the flag a non-`@Sendable` closure inherits the isolation of wherever it is written, so every bare closure inside a `@MainActor` type becomes main-actor-isolated; when the framework then calls it on a background queue the process dies. `PHPhotoLibrary.requestAuthorization`'s block carries no `NS_SWIFT_SENDABLE`, and two view models pass it a bare closure. Both are now `@Sendable`, which is right either way, but the flag is back off for the app and the tests: nothing in the toolchain points at the remaining sites, so switching it on is an audit of every closure handed to a framework callback, not a build setting. `MEMBER_IMPORT_VISIBILITY` is compile-time only and did go project-wide — it failed the build on twelve files reaching `@Published`, `ObservableObject`, `ButtonRole` or `UNAuthorizationStatus` through a re-export instead of importing `Combine`, `SwiftUI` or `UserNotifications`, all genuine and all now imported. The iOS 26 floor is deliberate, so the leftovers it made dead were removed: an `if #available(iOS 16.0, *)` fallback, `.autocapitalization`, eight `NavigationView`, and the `Alert` value type across eleven screens (see **D52**). | accepted |
| D52 | Button roles travel with the button, because the screen presenting an alert does not know which button is the dangerous one | Eleven screens each open-coded `.alert(item:)` against the `Alert` value type, both deprecated since iOS 15, and each picked the button roles itself. They disagreed: the same confirm button was `.destructive` on one screen and `.default` on another, and the Retry/Discard alert reddened **Retry** while leaving Discard plain. One `View.alert(state:)` modifier now renders every `AlertState`, and `AlertButton` carries a `role`, set where the code knows what the button does — `.destructive`/`.cancel` from `AlertState.confirmation`, which every caller uses to guard a destructive act, and `.destructive` on Discard. With no buttons the actions builder stays empty and SwiftUI supplies its own OK, which is what `Alert(title:message:)` did. | accepted |
| D53 | A test that starts a request must hold the stub for as long as the request lives, not merely while it asserts | `StubGate` (see the type's own note) serialises captures because `StubURLProtocol` registers process-wide. It was not enough. `ImageReloadTests` held no gate at all and called `AuthenticatedImageLoader.reload()`, which puts a real request on `URLSession.shared`; when that overlapped `EndpointShapeTests`' window the stub recorded it, and `captureOne` read `GET /api/i/failed` instead of the delete under test. The same suite also wiped the process-wide `ImageCache` while `ImageLoaderIsolationTests` was asserting an image was still in it. Both are fixed at the source: the request-firing tests run inside `StubServer.serving` so their request is answered and finished within their own window, the cache-touching ones inside a new `StubServer.holdingTheStub`, and `ImageLoaderIsolationTests` clears the cache inside the gate rather than before taking it. | accepted |
| D54 | A `@MainActor` closure handed to a framework that calls back on its own queue is a Swift 6 *runtime* trap, and the compiler never mentions it | `AppDelegate` conforms to `UIApplicationDelegate`, which the SDK annotates `@MainActor`, so the whole type — statics included — is main-actor isolated and the launch-handler closures in `registerBackgroundTasksEarly()` inherited that. They were registered with `using: nil`, which lets `BGTaskScheduler` call them on a queue of its own choosing. Swift 5 never checked; Swift 6 checks isolation at runtime, so `swift_task_isCurrentExecutorWithFlags` -> `dispatch_assert_queue` killed the app with `BUG IN CLIENT OF LIBDISPATCH: Assertion failed` the first time iOS actually ran a background refresh — about fifteen minutes in, on a device, on a path no test and no launch touches. Fixed by registering with `using: .main`, which also settles `BGTask.expirationHandler` (delivered on the same queue). Safe because neither handler works on that queue: each logs, reschedules, installs the expiration handler and hands off to a private `OperationQueue` or to `performBackgroundSync`. The general rule, and the sweep it implies: for every closure given to a framework callback, check `NS_SWIFT_SENDABLE` in the SDK header — without it the closure inherits the enclosing type's isolation and the delivery queue must be made to match. The audit found three such closures: this pair, and `PHPhotoLibrary.requestAuthorization` in two view models (now `@Sendable`). `Operation.completionBlock`, KVO's `observe` change handler, `NotificationCenter` observers and the `URLSession` delegates were all already safe — the first two because the SDK marks them `NS_SWIFT_SENDABLE`, the rest because they take `queue: .main` or live on types that are not main-actor isolated. Simulators cannot run `BGTaskScheduler` at all (`BGTaskSchedulerErrorCodeUnavailable`), so this class of bug cannot be caught anywhere but on a device. | accepted |
| D55 | Lazily-created per-user rows must be created in their own transaction, because a lost insert race takes the caller's whole transaction with it | `no_tag` is created on first use, so a brand-new user's *first* upload is the one that creates it. The iOS share sheet fires its TUS post-finish hooks concurrently: on 2026-08-28 six photos went up for a fresh account, three hooks landed in the same millisecond, all three read "tag missing", all three inserted, and the two losers took `Duplicate entry '3-no_tag' for key 'uk_user_tag_name'`. The violation is thrown inside the same transaction as the `file_metadata` insert, so the rollback took the asset row with it — four photos arrived, ids 6767/6768 were burned, and nothing surfaced to the user because tusd had already answered 204. Fixed by `SystemTagProvisioner`: the insert runs `REQUIRES_NEW`, so a violation poisons only that throw-away transaction, and the lost race re-reads in yet another fresh transaction (necessary, not decorative — MariaDB's REPEATABLE READ means the caller's open snapshot predates the winner's commit and cannot see the row). It hands back the tag *id*, and callers resolve it with `getReferenceById`, which issues no SELECT and so never meets that snapshot. Note this only ever bit new accounts; every existing user already had the row.  **Amended same day, after the first fix shipped and made it worse (3 of 6 lost, not 2).** Moving the insert to its own transaction stops the duplicate key from rolling the caller back, but it does not make the new row *referenceable*: the upload transaction had already opened, so under REPEATABLE READ its snapshot predates the tag's commit, and the `image_tags` insert's foreign-key check on that unseen parent fails with `1020 Record has changed since last read` — same rollback, same lost photo, one more of them, because this time even the race *winner* failed (its own upload transaction also predated its own `REQUIRES_NEW` commit). `getReferenceById` dodges the SELECT but not the FK check. The working fix is ordering, not isolation: `no_tag` is now provisioned *before* `transactionTemplate.execute` opens the upload transaction, so the snapshot always contains the tag it is about to reference. `SystemTagProvisioner` stays — the `REQUIRES_NEW` insert is still what keeps a lost race from poisoning anything. Residual and accepted: the `@Transactional` callers (`removeTagFromFile`, `addNoTagToFetchedFile`, `resolveTagForBulkOperation`) cannot hoist and could in principle hit the same 1020, but their lazy branch is unreachable in practice — a file you can untag was uploaded, and uploading is what creates `no_tag`. Also unfixed, and worth a separate look: a failed TUS post-finish only logs `register failed … orphan cleanup may be needed`; tusd has already answered 204, so the client is never told the photo did not land. | accepted |
| D56 | A client setting that blocks its own requests must be enforced *before* the request, because URLSession reports the block as a lie | With **Wi‑Fi Only** on, `URLRequest.applyNetworkPolicy()` sets `allowsCellularAccess`/`allowsExpensiveNetworkAccess`/`allowsConstrainedNetworkAccess` to false. On cellular URLSession then fails the task with `NSURLErrorNotConnectedToInternet` (-1009), whose `localizedDescription` is "The Internet connection appears to be offline" — untrue on a phone with five bars of 5G. The sync log rendered each one as a red "Failed to upload …", and three of them tripped `ExportRetryPolicy`'s give-up rule, which logged a second red line blaming *export* for a network refusal (reported 2026-08-29 from a background run). Nothing was lost — the assets are never marked uploaded — but the log taught the user to ignore red. Fixed by `NetworkMonitor` (`NWPathMonitor`): `drainQueue()` is the one door every export and every byte goes through, so the gate lives there and holds the queue intact instead of failing it. The link is classified by `isExpensive || isConstrained`, not by interface type — a personal hotspot is Wi-Fi by interface and cellular by cost, and Low Data Mode is Wi-Fi that URLSession still refuses — so the gate predicts URLSession's own answer rather than guessing at it. Two things are load-bearing: it **fails open** when no path reading exists yet (a broken monitor must not become an app that silently backs nothing up), and the monitor is wired in `SyncCoordinator.init`, not `start()`, because a background launch never calls `start()` — and background is exactly where this was reported. Resume is driven by both the path handler and the Wi‑Fi Only toggle. | accepted |
| D57 | An album becomes public when its owner says so, not when it is created | The share token is minted in `createAlbum`, so every album was reachable by link from the moment it existed — before a single photo was in it — and `AlbumSubscriptionNotificationService` would announce it to subscribers of the owner's other albums on the same footing. `albums.published` (V43, default FALSE) separates "has a link" from "is public". Enforcement is a repository query, not a scattering of `if`s: `findByShareTokenAndPublishedTrue` replaces `findByShareToken` at every anonymous entry point — the album, its files, its presentation groups, its recordings, the analytics beacons, the OG page and subscribing — so an unpublished album is an empty `Optional` and therefore the same 404 as a token that was never issued. A visitor cannot distinguish "hidden right now" from "no such album", which is the point. Three deliberate exceptions: **unsubscribe** stays on the ungated lookup, because someone holding an unsubscribe link must always be able to get out; **`/api/i/{token}`** is not gated, since the owner's own gallery serves its thumbnails through it and per-file tokens are unguessable and only discoverable through the album listing this *does* gate; and **duplicates** start unpublished however public their source was, since a copy is about to be re-curated. `published_at` is stamped on first publish only and survives unpublishing, so the "new albums from this owner" notifier announces an album on the day it goes live rather than the day it was created, and a republish does not re-announce it. Existing albums were backfilled to published: they were already reachable, and turning them off would have broken links people hold. | accepted |
| D58 | A credential that is baked into the artifact cannot be rotated, so it is not really a secret | The APNs signing key was committed in the first commit (2025-12-17) at `server/src/main/resources/AuthKey_W4CMQFDV9D.p8`, and `ApnsService` resolved it as a `ClassPathResource` — copying it out of the JAR to a temp file at boot. That put a live Apple credential in git history, in every image layer, and behind a rebuild-and-redeploy for any rotation. The MapKit key next to it already had the right shape, so this is a straight copy of it: `app.apns.private-key` takes the `.p8` body, the chart puts it in `photo-upload-secret` under `apns.privateKey` via `--set-file`, and the api reads it from `APNS_PRIVATE_KEY`. Three things came with it. `isConfigured()` makes "no key" a supported state that logs once and drops pushes, rather than an exception every boot — the api serves a gallery, and a notification channel must not be able to take it down. `app.apns.key-path` survives as a *filesystem* path (never a classpath name) for local dev, because pasting a multi-line PEM into a shell is worse than pointing at a file. And the ignore rule lost its leading slash: `/AuthKey_*.p8` only ever watched the repo root, which is precisely why a key under `server/src/main/resources` could be committed in the first place. The file itself moved to the repo root beside the MapKit key, so both Apple keys are in one place and one rule covers both. The committed key stayed readable in git history, so the move alone fixed nothing — it was revoked in the Apple portal on 2026-08-30 and replaced by `289ZRKLFNQ`, which has never been in the repo. The dead one in history is now just bytes. | accepted |
| D59 | Storage is per album, chosen once, and a user may bring their own | Every byte went to the one MinIO named by `storage.s3.*` — the operator's disk, the operator's bill. `storage_backends` (V44) lets a user register their own S3-compatible endpoint and point new albums at it; the metadata database stays here, the expensive part becomes theirs. Four decisions hold the design up. **The system default is a row with no credentials in it**: `system_default = TRUE`, `user_id NULL`, endpoint/bucket/keys deliberately null and resolved from `storage.s3.*` at runtime, so the cluster's MinIO secret is never copied into a table. **The choice is permanent**: `updateAlbum` rejects a different `storageBackendId` rather than moving objects, because a half-moved album has no correct answer for a presigned URL, and `duplicateAlbum` is pinned to the source's backend since a copy reuses the source's storage keys rather than re-uploading. **tusd stays on the system default**: it is one process with one set of credentials and it does not know which album an upload is for, so the post-finish hook is where the bytes reach the album's real backend — same backend is still a server-side COPY, a different one streams through the api pod, which is the unavoidable price of bring-your-own-storage. **Every sweep runs a bucket at a time**: `purgeOrphanedS3Objects` and the retention orphan pass compare one backend's listing against only the albums that live in it, because a key missing from the DB is garbage relative to its own bucket only, and a backend whose listing failed must be skipped rather than read as empty. Secondary but load-bearing: each backend gets its own circuit breaker (`s3-backend-{id}`) so one user's dead endpoint cannot open the breaker that guards everyone else's uploads, and only the `minio` breaker feeds the readiness probe. Secret access keys are AES-GCM encrypted with `storage.backend-secret-key`; without that setting the instance still runs on its own MinIO and the API says the feature is unavailable instead of failing at upload time. Every write path proves the endpoint with a real PUT/HEAD/DELETE round trip before saving — a HEAD alone passes with read-only credentials and then loses every upload. | accepted |
| D60 | Storage on the instance's own disk is capped per user; storage on theirs is not | **D59** made the bytes optionally someone else's problem but left the default path uncapped — any account could fill the operator's MinIO. `users.storage_quota_bytes` (V45, default 100 MiB) caps what a user may keep on the *system* backend only. An album on their own S3 is neither counted nor capped, because capping it would be us rationing a disk we do not pay for. Three things make the number mean something. **Derivatives are tracked, not inferred**: `file_metadata.derivative_bytes` is summed as the worker writes thumb/medium/large/transcoded and reset whenever it rebuilds them, because retention deletes originals after a week — a quota that counted only `file_size` would read as nearly empty on a month-old account while its thumbnails and transcoded videos sat on the disk for good (measured: a 15 KB clip leaves 37 KB of derivatives, so the untracked part is the larger one). Narration gets the same treatment in `slideshow_recordings.audio_bytes`. **Usage counts objects, not rows**: both sums are `DISTINCT` on the storage key, because `duplicateAlbum` copies metadata rows that point at the same objects, and a user who duplicated an album would otherwise appear to have doubled their usage without a byte being written. **Transport is free**: the `tus-uploads/` staging prefix is never counted — bytes there live for minutes, are swept by retention whether or not the upload finished, and an album on a user's own storage passes its bytes through that prefix on the way out; billing it would charge for transport. Enforcement sits at four doors: TUS pre-create (so a phone on a train does not spend ten minutes uploading a video with nowhere to land — advisory, since the size is the client's declared `Upload-Length`), TUS post-finish (authoritative, and a rejection leaves the object in `tus-uploads/` for retention rather than charging for it), multipart upload, and narration upload. Refusals are **507**, not 400 or 413: the request was well-formed and the file was fine, and a client must show the message rather than back off and retry, which is what it would do for an unrecognised 5xx. Usage is computed on demand rather than kept in a counter column — two uploads racing a nearly-full quota can both pass and land slightly over, which is accepted: a counter would need locking on the hottest path in the app to buy a precision nobody asked for. Rows written before V45 meter as free until `POST /api/admin/recalculate-derivative-bytes` reads their real sizes out of the bucket; without that one-off run an existing install's quota measures almost nothing. There is deliberately no API for changing a quota — `UPDATE users SET storage_quota_bytes = …` — and 0 is a usable way to freeze an account. | accepted |
| D61 | TUS uploads carry the client's SHA-256, and it dedupes per user | The TUS hook deduplicated on `contentId` alone (**D30**), and `registerTusUpload` never called `setChecksum`, so every row that path created had a null checksum. On iOS a `contentId` is `PHAsset.localIdentifier` — an identifier the *device* mints for its own copy of a photo, so an iPhone and an iPad hold different ones for the same iCloud asset and the check can never match across them. `UploadRouting.selectPath` sends everything to TUS whenever the server advertises it, with no size threshold, and the deployed `TUS_ADVERTISED=true` does; so the multipart path's checksum sweep — the one thing that *would* have matched — was unreachable in production, and an account synced from two devices duplicated every asset. The client now puts its SHA-256 in `Upload-Metadata.checksum`. It already computed one: `exportAssetToTempFile` hashes the bytes it wrote, and those bytes are the untouched original resource from `PHAssetResourceManager`, identical on both devices — the reason the hash works where the id cannot. Three gates use it: pre-create rejects a known checksum with **409** before tusd allocates the multipart upload, which is what keeps a second phone's back-catalogue re-sync cheap; post-finish re-checks after the bytes land, because two devices can clear pre-create in the same instant, and the loser is dropped without a row (its `tus-uploads/` object goes to the nightly retention sweep, the same outcome the duplicate-`contentId` branch already accepted); and `registerTusUpload` persists the value so the *next* upload can match it. The lookup is `findByChecksumAndUserId`, deliberately not the multipart path's `findByChecksum`, which is account-blind — refusing an upload because a stranger holds the same bytes would both leak that fact and lose the user's photo. Nothing is recomputed server-side: on the same-backend path the bytes never enter the pod (S3 COPY), so hashing here would mean pulling the whole object back to confirm what the client already said. A blank checksum means "unknown", never "matches everything", so an older client keeps uploading; rows written before this change keep their null checksum and were cleaned up by hand. | accepted |
| D62 | A request that must survive app suspension belongs on the background session, and the one exception was the request that starts every upload | The TUS PATCH and the resume HEAD were both on the background `URLSession`; the create `POST /files/` was still on `URLSession.shared` (the HEAD's own doc comment already spelled out why that cannot work). iOS suspends the app a few seconds into a `BGProcessingTask`, kills every `URLSession.shared` task with it, and delivers the error only at the next wake-up — as `NSURLErrorTimedOut`, "The request timed out." So the Status tab filled with red lines timestamped to the instant the app woke, and after three of them `ExportRetryPolicy` gave the asset up with a second line blaming *export*: the same misattribution **D56** fixed for Wi‑Fi Only, arrived at from a different direction. The server logs settle which side failed — over the reported window tusd logged zero errors, zero non‑2xx and answered every POST it received, and there was no POST at all between 19:10:32Z and 19:31:59Z while the phone recorded failures at 19:31:57Z: the requests died on the device. Nothing was lost, since the assets are never marked uploaded, but background sync could not make progress past its first suspension. Fixed by sending the create as a background **upload task with a zero-byte body** — background sessions accept only upload and download tasks, and TUS creation carries its length in `Upload-Length`, not in the body, so the empty file costs nothing and is cleaned up as the task's `chunkPath`. Two consequences follow from a background session having no per-request completion handler. The 507 body, which is the only thing that names how much room is left, is now accumulated in `URLSessionDataDelegate.didReceive` and read once when the task ends. And the live objects the answer needs — the `PHAsset`, the export record, the caller's completion — cannot go in `taskDescription`, so they are held in an in-memory `pendingCreates` map: present for the whole life of the process, absent after a relaunch. That absence is a supported state, not an error. A 201 still continues, because path, checksum and size are all `patchChunk` needs and `TusTask` already carries them; a 409 still records the dedupe; everything else cleans the export up rather than leaving the asset stuck in `uploading`. | accepted |
| D63 | One product name, `Picz`, everywhere a user can read it — identifiers keep their old names | The name had drifted into four: `Picz` (landing page, tab title, app headers, iOS welcome screen), `Picz2` (auth pages, Terms/Privacy/Imprint, every transactional e-mail), `Photo Gallery` (`og:site_name` on shared album and image previews, so every link shared to a chat app announced a name that appears nowhere in the product) and `Zyncloud` (the iOS home-screen name, its two permission prompts, four in-app permission-error strings). The share extension added a fifth, `Photo Upload`. All user-visible strings now read `Picz`. Identifiers were left alone on purpose — the `ios/Zyncloud/` path, the Xcode target/scheme, `PRODUCT_BUNDLE_IDENTIFIER`, the `com.oglimmer.photosync` keychain service and its `PhotoUploadCredentials` legacy service — because a keychain service rename logs out every installed device, and the bundle id is the App Store identity. `picz2.oglimmer.com` and the `picz2-be`/`picz2-fe` images stay too: infrastructure names, not brand. Released as 2.0.0. |
| D64 | The standalone macOS client is deleted rather than carried | `UploadExtension/` was a second Cocoa app with its own Xcode project, orphaned since 2026-05-04 — two commits ever, built by nothing, referenced by nothing outside a history line, bundle id still `com.example.PhotoUploader`, and 17 scaffolding markdown files (`COMPLETE.md`, `FIX_APPLIED.md`, `BUILD_STATUS.md`, …) around ~600 lines of real code. The 2026-08-21 iOS review had already called it dead. It was still *functional* — legacy `email:password` TUS auth is accepted (**D44**) and `checksum` is optional (**D61**) — so this is a maintenance decision, not a compatibility one: a client nobody builds is a client nobody notices breaking, and it had already drifted past the **D63** rename. macOS keeps the web UI. History holds the code at `1a2265b`. |
| D65 | An endpoint with no caller in *either* client is deleted, not kept for symmetry | The three components had drifted apart: the server carried nine routes nothing requested, several of them the id-scoped twin of a public-token route both clients actually use (narration audio is fetched at `/api/r/{token}/audio` by the web app and by iOS, never at `/api/recordings/{id}/audio`, because `AVPlayer` and `<audio>` both fetch on their own and cannot carry Basic auth). Keeping the unused twin is not free: it is a second authorisation path over the same rows that no test and no client exercises. The check that matters is that it runs across all three components at once — `GET /api/sync/uploaded-checksums` reads as dead from the server and from the web app and is called by iOS on every sync, and it was nearly deleted because iOS writes request paths without a leading slash (`"api/sync/..."`) and so does not match the `/api/` grep that finds every web-app call. Operator endpoints are the standing exception: `/api/admin/*` has no client by design and is documented as curl in the Helm README. |
| D66 | v1 data is migrated per account, straight into the database and the bucket, and in two passes because the source bytes carry no EXIF | Runbook: `bin/migrate-v1.md`; scripts: `bin/migrate_v1.py`, `bin/migrate-v1-run.sh`. **Written, not run.** Three things decide the shape. **Per account, and only `oglimmer@gmail.com`**: v1 has 73 users, but it authenticates them against Keycloak and Apple while v2 authenticates against `users.password`, so a migrated account cannot log in here without a password reset regardless — and that one address (v1 ids 7 *and* 89, the same person through both issuers) owns 3459 of the 3629 photos, while 34 of the other accounts own none. **Direct writes, not `POST /api/upload`**: the API path needs each user's v2 password, which for the same reason does not exist. So the script inserts `file_metadata` itself and must reproduce by hand what `FileStorageService.storeFile` does around it — the `originals/{stored_filename}` key, the `no_tag` row (**D55**), the QUEUED `PROCESS` job. **Two passes**: `ImageResizeService.removeExif` strips EXIF before v1 ever stores an object, so date and GPS have to come from the v1 *database*, and `FileProcessingService` writes `gps_latitude`/`gps_longitude` unconditionally from what it extracts — `NULL` included — so a backfill before the worker runs would be erased by it. Upload first, wait for `DONE`, then backfill. Timezones are a non-problem worth writing down rather than rediscovering: the v1 api pod runs in UTC and metadata-extractor's `getDateOriginal()` carries no zone, so v1's `creation_date` is the camera's wall clock stored as a naive UTC datetime — written through unchanged with `capture_utc_offset_seconds = 0`, which is what keeps v2's group-by-day on the day the shutter fired. `content_id = 'piczv1:<v1 element id>'` makes the run idempotent, resumable, and reversible with one `DELETE`. Accepted losses: 182 `SECTION` and 68 `MAP` elements have no v2 equivalent (`presentation_groups` is anchored to a tag, and v2's map is per-album and driven by real GPS), and 268 per-photo captions have nowhere to go — `file_metadata` carries no caption column. Also accepted: the migrated "original" is already a derivative, since v1 kept only its 1200px/q0.4 resize in S3. | pending |
| D67 | The web app's three-step thumbnail switch is on iOS too, and on iOS a pinch is half of it | The web UI has had a small/medium/large picker over the album shelf and over an album's photos since it shipped; the iOS grids were fixed at whatever `AdaptiveGrid` returned for the size class. `GridSizeMode` adds the three steps and `AdaptiveGrid` takes it as a second input, so a phone photo grid is 5/3/2 columns and an iPad's is 9/6/3, cards 3/2/1 and 6/4/2. Two things were deliberate. **`.medium` is the default and draws exactly the old counts**, so no install re-lays-out on upgrade — `AdaptiveGridTests` pins that. **Two entry points, because iOS has two conventions and the web only has one**: an inline `Picker` inside the toolbar menu, which is where iOS puts view options, *and* pinch-to-zoom on the grid, which is what Photos has trained everyone to try. The pinch is a one-step ratchet rather than a continuous scale — the layout underneath is a column count, not a zoom factor — and it latches only on a step that actually happened, so pinching past either end neither locks the gesture nor fires haptics. Stored per screen (`albumGridSize`, `photoGridSize`) like the web's two localStorage keys, because browsing a shelf of albums and browsing photos are different habits. Tag chips are dropped at `.small` whatever the Show Tags toggle says: five tiles across a phone leaves a tag name about a centimetre wide. Thumbnail renditions are untouched — the server's `THUMBNAIL` is 600px, which still covers a 2-column phone tile. | done |
| D68 | Every new asset carries the `all` tag; the `no_tag` marker is gone | `no_tag` was an internal flag meaning "this asset has no real tag". Every client hid it (`useFiles.ts`, `PublicGalleryView.vue`, `Photo.visibleTags`, `PresentationGallery.tagCounts`), no SQL ever selected on it, and its only functional reader was `AlbumSubscriptionNotificationService`'s visible-image heuristic. It was pure bookkeeping, so it is deleted rather than kept in step with the new rule. In its place `all` — already a per-user system tag, already implicitly enabled on every album — is attached to every asset at registration: both upload paths in `FileStorageService`, plus the album-duplicate loop in `AlbumService`. `all` counts as an ordinary tag now, so adding a second tag no longer strips anything and removing the last one no longer restores anything; a file with zero tags is a legal state. `all` stays protected in the sense that matters: `isSystemTag` still shields it from rename/delete and from the per-album enabled-tag check, so an upload can never be refused the tag. **Existing assets are not backfilled** (`V46__drop_no_tag.sql` deletes the `no_tag` rows and stops there) — a deliberate call, taken knowing the cost: `PublicGalleryView.vue:591` auto-selects the tag when an album has exactly one, so a shared album holding both pre- and post-migration photos will land on `all` and show only the newer ones until the older are tagged by hand. | done |
| D69 | A photo carries one owner-written caption, shown to public visitors in both the grid and the zoomed view | Asked for as "comments on individual images", and deliberately built as a **caption, not a comment thread**: the owner writes it, everybody reads it. That decides the whole shape — one nullable `caption` TEXT column on `file_metadata` (`V47__add_file_caption.sql`), no author, no timestamp, no second table, and nothing for a public visitor to POST to, so the share link gains no spam surface and no moderation screen. It rides out on `FileInfo`, which the public album endpoint already returns, so the read side needed no new endpoint at all; the write side is one synchronous `PUT /api/files/{id}/caption` answering with the updated asset, because unlike a rotate nothing is re-rendered and there is no status to poll. Blank in stores NULL, so "no caption" has exactly one representation and every reader tests for null alone; 2000 characters is the cap, chosen for the layout rather than the column. Shown in three places per client: under the thumbnail in the grid (clamped — 3 lines on the web, 2 on iOS, so one long caption cannot make its tile twice its neighbours' height; on the web the clamped element takes its vertical space as **margin, never padding**, because `-webkit-line-clamp` clips at the padding box and a fourth line stays visible inside any bottom padding, bleeding out of the card), over the bottom of the picture in the lightbox / full-screen view (hidden by "Hide Controls" during slideshow playback, like the group hint; clamped to 3 lines with a Show more toggle borrowed from that hint, left-aligned rather than centred because a centred paragraph over a photo reads badly, and the height cap sits on the *text* so the toggle does not scroll away with the words above it), and as the Open Graph description of a shared single image, where it beats the camera's `IMG_1234`. Editing is inline on the web tile and a sheet on iOS — a caption is a sentence or two, and on a phone an inline box would be typed into behind the keyboard. Note for **D66**: the v1 migration listed 268 per-photo captions as an accepted loss because `file_metadata` had no caption column. It has one now, so that loss is recoverable if the migration is re-run. | done |
| D70 | New uploads land on a per-user tag, defaulting to a `hidden` holding pen | Fixes a real hole opened by **D68**: since every new asset got `all`, and `all` is what public visitors filter on, a photo the phone auto-uploaded was on a published album's share link within seconds — no review, no way to stop it short of unpublishing the album. The tag a new asset gets is now a per-user choice between two system tags, held on `users.new_asset_tag` (`V48`), and `hidden` is the default. **`hidden` is a second system tag, not a flag on the asset.** It costs one row in a table that already exists, it is already understood by every tag-shaped thing in all three clients, and "publish this photo" is then an ordinary tag removal the owner already knows how to do — including the album-wide Remove button, which becomes a one-click publish-everything. A boolean column would have needed its own editor, its own bulk endpoint and its own iOS control. **The gate is the listing, not the image route.** `listFilesByAlbumByShareToken` drops hidden assets and `getFileInfoByPublicToken` 404s on one, so a visitor is never handed the token; `/api/i/{token}` is deliberately left open, because the owner's own web gallery and the iOS app fetch every thumbnail through it and gating it would blank their grid. `PublicGalleryView` filters again on arrival — the same door, locked twice, because the cost of the server-side filter regressing one day is somebody's private photos on a public page. Subscription mails count the visible set only, so the "new photos in this album" mail now fires when the owner publishes rather than when the shutter clicked. **Every existing user was moved to `hidden`, not just new ones** — leaving current accounts on `all` would have left the hole open for exactly the people who already have published albums. Nothing already uploaded is re-tagged in either direction: the setting decides what happens next, and a settings toggle that silently pulls hundreds of live photos off a shared page (or pushes them onto one) is not a settings toggle. The switch **towards** `all` needs a confirmation and the server enforces that — `PUT /api/settings/new-asset-tag` refuses `all` without `confirmed: true`, so a client that forgets its dialog gets a 400 rather than quietly opening the album up; the switch back to `hidden` needs none, because it can only ever narrow what visitors see. `V48` also renames any pre-existing ordinary tag called `hidden` to `hidden-<id>`, or that user's photos would have vanished from their own public album on deploy. | done |
| D73 | A chapter can end without the next one starting | Until now a presentation group was only a *start*: the walk handed every image to the last anchor it passed, so the final chapter of a tag always ran to the end of the album and there was no way to say "this section is over". `presentation_groups.end_file_id` (`V49`, nullable) names the **last image still in the chapter**; null keeps the original open-ended behaviour, which is what every pre-existing row gets and what a chapter created without an end still does. **A nullable column on the group, not a second marker row.** The alternative was a sentinel row meaning "stop here", which would have forced `label` nullable and made every read path skip rows that are not really chapters — a group is one row and stays one row. **Position is never validated.** Display order lives in the images and can change under a group at any time, so "the end comes after the start" is not a durable invariant the server could hold; instead both walks simply never reach an end they pass out of order, and the chapter stays open. Same reasoning makes `ON DELETE SET NULL` the right cascade for `fk_pg_end_file`: losing the image a chapter ends at must reopen the chapter, not delete it — unlike the anchor, whose `CASCADE` is correct because a section with no start cannot render at all. **The end has its own endpoint** (`PUT /api/presentation-groups/{id}/end`) rather than a field on the update body, because `updateGroup` is a partial PUT that ignores tag and anchor, and the iOS client sends only label and text — folding the end in would have meant every edit of the words silently cleared the end marker. Create *does* take `endFileId`, so a chapter can be born covering exactly the images wanted. One consequence had to be fixed in both clients: a chapter that ends leaves a headingless run behind it, so there can now be several of those in one presentation, and the section keys/ids that assumed a single leading run would have dropped sections. | done |
| D78 | The browser holds a revocable session token, never the account password | The web app kept the plaintext password in `localStorage` and sent it as Basic auth on every call. D44 had already rejected "base64 is not encryption" for the tusd metadata, and the same reasoning applies one layer up: any XSS was a permanent account takeover, and there was nothing the server could revoke short of the user changing their password. `POST /api/auth/sessions` is the one Basic-authenticated request the browser makes; it answers with a `zst_…` token (`session_tokens`, `V52`, SHA-256 only — same shape as `upload_tokens`) that goes into `localStorage` in the password's place and rides as `Authorization: Bearer` from then on. `SessionTokenAuthenticationFilter` resolves it ahead of the Basic filter with the same roles `CustomUserDetailsService` would grant, so D74 means the same thing on both paths. `DELETE /api/auth/sessions/current` is logout; `UserService.revokeUploadTokens` now also deletes every session on a password change or reset, so "I changed my password" once more locks out whatever had the old credentials (the web app signs itself back in with the new one). TTL 30 days (`SESSION_TOKEN_TTL_DAYS`, `backend.sessionTokenTtlDays`). A browser still holding the old `authPassword` trades it for a session once on start-up and deletes it. iOS is untouched: Basic stays accepted everywhere, and the TUS `auth` metadata now carries only the scoped upload token — the browser has no password left to fall back to. | done |
| D79 | `hidden` is derived from "has no other tag", not assigned | D70 made `hidden` an ordinary tag the owner takes off to publish. In use that produced two states nobody wanted: a photo tagged `beach` that stayed invisible because `hidden` was still on it, and a photo with no tag at all that was public because nothing had put `hidden` back. The rule is now one sentence: **a photo carries `hidden` exactly while it has no other tag.** `FileStorageService.addTagToFile` and `addTagToAllFilesInAlbum` drop `hidden` in the same transaction as the add — also on a file that already had the tag next to `hidden`, so a bulk add is the repair for older rows; `removeTagFromFile` and `removeTagFromAllFilesInAlbum` put `hidden` back on a file left bare; `TagService.deleteTag` does the same for files whose only tag was the deleted one (`ImageTagRepository.findFileIdsWhereTagIsTheOnlyOne`), or deleting a tag would have quietly published every photo that had nothing else. **`hidden` is refused as an add** (single and bulk): it would be a no-op or recreate the tagged-but-hidden state. **A lone `hidden` is refused as a remove** with a message that says to tag the photo instead, because it would come straight back; `hidden` next to a real tag can still be removed by hand, and the bulk remove skips the lone ones instead of churning them. **Still a tag, not a column** — the D70 reasoning holds, and the derived rule costs one extra lookup per tag edit. **No data migration.** Stripping `hidden` from every tagged photo would push private photos onto share links; adding it to every bare photo would pull public ones off. Both are exactly what D70 refused to do from a settings toggle, so older rows keep their state until the owner next touches them. **Enforced on the server, mirrored in the clients.** The web tag select, quick tags and tag-all dropdown and the iOS one-photo and bulk sheets no longer list `hidden`; the filter still does, so the owner can find the holding pen. An older client that still sends it gets the 400 and its message. "Add `all` to every photo" is the one-click publish now; "remove `hidden` from every photo" publishes only what already has another tag. `TagService` became `@Profile(API)` because it now needs `SystemTagProvisioner`, which only the api pod has; nothing on the worker used it. | done |

---

## Gap 6 — Explicit processing status field (Phase 0)

- [x] Migration `V30__add_processing_status_to_file_metadata.sql`: adds `processing_status`, `processing_attempts`, `processing_error`, `processing_completed_at`. Backfill `DONE` where any derivative exists, else `FAILED`. Index on status.
- [x] Entity: `FileMetadata` gains a `ProcessingStatus` enum (`@Enumerated(STRING)`).
- [x] `FileStorageService.storeFile`: sets `QUEUED` (dispatcher enabled) / `INGESTED` (legacy) before save.
- [x] `FileProcessingService.processFile`: transitions `PROCESSING → DONE/FAILED`; increments attempts on error.
- [x] `UploadController.uploadFile`: returns `202 Accepted` with `processingStatus` in body.
- [x] `AssetStatusController` exposes `GET /api/assets/{id}/status`.
- [ ] iOS: optional follow-up to render the status in the gallery (no compatibility break — `Uploader.urlSession` already accepts the full `2xx` range).

### Testing
- [ ] Migration smoke test against a prod-snapshot DB.
- [ ] Status-transition unit tests.
- [ ] Integration test: upload a file and assert `INGESTED → DONE`.

### Risk / rollback
200 → 202 is a contract change. Web frontend must be checked. Rollback by reverting the controller alone (DB columns stay).

---

## Gap 5 — libvips for image thumbnails (Phase 0)

- [x] Refactor `ThumbnailService` into `VipsThumbnailService` + `HeicConversionService` + `FfmpegService` (D14). All three classes shipped; `ThumbnailService.generateAllThumbnails` dispatches to vips by default.
- [x] `Dockerfile`: installs `libvips-tools` (and a hand-built libheif/ImageMagick chain for the HEIC fallback).
- [x] Feature flag: `file.upload.thumbnailer=vips|magick`. Default `VIPS` (set in `FileStorageProperties.Thumbnailer`).

### Testing
- [ ] Golden-file thumbnails for JPEG / 50MP JPEG / PNG / animated GIF / EXIF-rotated input.
- [ ] RSS comparison via `jcmd VM.native_memory summary` before/after.

### Risk / rollback
vips handles unusual color profiles slightly differently. Flag-gated rollback.

---

## Gap 4-prep — PVC + MinIO alert (Phase 0, independent)

- [x] Alert rule body authored. **Substitution**: the cluster runs the standalone `prometheus` chart, *not* `prometheus-operator`, so the original `prometheusrule-storage.yaml` template was removed. The rule YAML now lives in `NOTES.txt` for one-time paste into the prometheus chart's `serverFiles.alerting_rules.yml`. Functionally equivalent; ownership boundary is the prometheus release rather than the photo-upload release.

---

## Gap 1 — Persistent jobs table with leases (Phase 1)

- [x] Migration `V31__create_processing_jobs.sql`: `processing_jobs` table with `(id, asset_id, status, attempts, max_attempts, leased_until, leased_by, last_error, created_at, started_at, finished_at)`, FK to `file_metadata` with `ON DELETE CASCADE`, composite index `(status, leased_until)`. Backfill enqueues every row left FAILED by V30 and resets their `processing_status` to QUEUED.
- [x] `ProcessingJob` entity, `ProcessingJobRepository` with native `SELECT ... FOR UPDATE SKIP LOCKED` for atomic lease acquisition.
- [x] `JobDispatcher` with `@Scheduled(fixedDelay=2000)`: acquires `Semaphore(1)` permit, leases a row in one TX (`status='PROCESSING'`, `leased_until=NOW()+15min`), runs `FileProcessingService.processFile` synchronously, mirrors the asset's resulting `ProcessingStatus` onto the job row, marking `DONE` / `FAILED` (→ `DEAD_LETTER` after max attempts).
- [x] `JobEnqueueService.enqueue(assetId)` called by `FileStorageService.storeFile` *inside the same TX as the metadata insert* → atomic.
- [x] `FileProcessingService.processFile`: synchronous core; `processFileAsync` thin `@Async` wrapper kept only for the legacy flag-off path.
- [x] `UploadBackpressureFilter`: switched to a **cached gauge** (`JobQueueDepthService`) of `(QUEUED + PROCESSING)` jobs, refreshed every 1 s via `@Scheduled`. Threshold stays at 200. Falls back to executor-queue check when the dispatcher is disabled.
- [x] Config: `jobs.poll.interval-ms`, `jobs.lease.seconds`, `jobs.max-attempts`, `jobs.backpressure.queue-depth-threshold`, `jobs.backpressure.refresh-ms`.
- [x] Feature flag `jobs.dispatcher.enabled` so we can fall back to old `@Async` for one release.
- [x] D15 admin surface: `GET /api/admin/dead-letter` (re-enqueue UI deferred per the decision).
- [~] Pre-deploy: `ProcessingJobLeaseTest` (Testcontainers, MariaDB 11.8) covers SKIP LOCKED non-duplication, expired-lease recovery, active-lease respect, V31 backfill. Gated by `-Drun.testcontainers=true` because the local Docker Desktop install returns stub responses to docker-java; IT runs cleanly on a daemon without that filter (CI / fresh Docker).

### Testing
- [ ] Testcontainers-MariaDB integration: two competing threads acquire from the same row → exactly one wins.
- [ ] Crash-recovery: abort mid-job, advance clock past lease, assert re-acquisition.
- [ ] Load: 500 fake assets, no duplicate processing.

### Risk / rollback
`FOR UPDATE SKIP LOCKED` requires MariaDB ≥ 10.6. **Verify the live version before merging.** If older, fall back to advisory lock or `FOR UPDATE NOWAIT` with retry. Rollback by flipping the feature flag.

---

## Gap 7 — Prometheus queue-depth gauge (Phase 2)

- [x] `JobMetricsConfig` registers one Micrometer `Gauge` per `JobStatus` value, all sharing the metric name `photoupload.jobs.queued` with a `status` tag (renders as `photoupload_jobs_queued{status=...}` in Prometheus).
- [x] One scheduled `GROUP BY status` query refreshes a `volatile EnumMap` snapshot in `JobQueueDepthService`. Both the gauge suppliers and the backpressure filter read from that snapshot — no per-scrape DB cost, no Caffeine needed.
- [x] `prometheus` added to actuator exposure; `micrometer-registry-prometheus` on the classpath.
- [x] Helm: scrape discovery via annotation-based mechanism (cluster has standalone `prometheus`, no operator). A headless `*-metrics` Service with `prometheus.io/scrape="true"`, `prometheus.io/port="8081"`, `prometheus.io/path="/actuator/prometheus"` is picked up by the existing `kubernetes-service-endpoints` SD job. (Replaces the original `PodMonitor` template, which the operator-less cluster could not reconcile.)
- [x] Helm: alert-rule YAML for jobs (`PhotoUploadJobsBacklog`, `PhotoUploadJobsDeadLetter`) emitted via `NOTES.txt` for paste into the prometheus chart's `serverFiles.alerting_rules.yml`. (Replaces the original `prometheusrule-jobs.yaml` template, same reason.)

### Risk
Management port (8081) is already internal-only; no exposure concern.

---

## Gap 2a — Introduce MinIO (Phase 3)

MinIO already exists in-cluster at `minio.minio.svc.cluster.local:9000`, so the Helm work for MinIO itself (Deployment / Service / PVC) is owned by the platform side. The plan below tracks only the application changes.

### Phase 3a — Foundation (landed)

- [x] Skip `deployment-minio.yaml` / `service-minio.yaml` / `persistentvolumeclaim-minio.yaml` — MinIO is platform-managed.
- [x] `software.amazon.awssdk:s3` + `apache-client` via the AWS SDK BOM.
- [x] `ObjectStorageProperties`, `ObjectStorageConfig` (S3Client + S3Presigner beans, both `@ConditionalOnProperty(storage.s3.enabled)` so local dev without MinIO still boots).
- [x] `ObjectStorageService`: `putFile(key, Path, mime)` via `RequestBody.fromFile` (never reads into heap), `getToFile`, `openStream`, `exists`, `delete`, `presignGet`.
- [x] `BucketBootstrapper` `@PostConstruct` — creates the bucket if missing (idempotent), fails startup loudly if creds are wrong.
- [x] Helm: secret entries for access/secret keys, configmap entries for endpoint/bucket/region, env wiring on the backend Deployment. Gated by `objectStorage.enabled` so the section is no-op when MinIO isn't desired.

### Phase 3b — Switch the data plane (all three pieces landed)

- [x] `FileStorageService.storeFile`: **direct-to-S3**, no durable disk write under app control. Reads `MultipartFile.getInputStream()` once for SHA-256 (Spring stages it transiently in `.multipart-tmp`), runs duplicate detection, then a second `getInputStream()` is piped to `objectStorage.putStream("originals/{stored_filename}", in, contentLength, contentType)`. Row saved with `filePath = "originals/{stored_filename}"`. Falls back to the legacy disk pattern when `ObjectStorageService` isn't injected (tests / non-S3 deploys).
- [x] `FileProcessingService.processFile`: per-job temp workdir under `{uploadDir}/.processing-tmp/{id}/`. Original downloaded from S3 (when `filePath` is an S3 key), HEIC→JPEG converts locally and uploads the new key (and deletes the legacy HEIC key from S3), thumb/medium/large/transcoded run on local temps and each is `putFile`'d to its `derivatives/{assetId}/...` key. Workdir is wiped via `deleteRecursive` in `finally`. `file_size` updated to the post-HEIC JPEG size (closes the stale-column source for new uploads).
- [x] **Serve layer** (read side) routed through `StoragePaths.isS3Key`. `ImageServeController` streams from `ObjectStorageService.openStream` when `FileServeInfo.storageKey` is set, otherwise serves the legacy disk Path. Both shapes coexist transparently — no flag day. (`FileController.downloadFile` legacy by-filename endpoint deferred — niche, not used by gallery.)
- [x] **End-to-end smoke**: id=4989 / id=4990 uploads both landed `originals/...` + three derivatives; zero durable bytes on disk; serve via API → MinIO returns byte-identical bytes (sha256 match local-disk-as-known-source).

### Phase 3c — Admin-driven migration (landed)

- [x] `MigrationService` (admin-triggered, not a CommandLineRunner — operator-paced is safer for an oversubscribed Pi). Single-thread executor, `AtomicReference<MigrationRun>` for run state, cancel checked at row boundary.
- [x] **Failure-safety properties**:
  - Idempotent per file: `objectStorage.exists(key)` HEAD-check before every PUT, so a re-run after a crash never re-uploads bytes already in S3.
  - Atomic per row: all five paths flip in a single TX. A row is never half-migrated.
  - Resumable via DB state: rows whose `filePath` already starts with `originals/` are filtered out by the repository query; re-runs converge naturally.
  - Skips `processing_status != DONE` rows so the migrator never races the dispatcher.
  - Local files are *kept* (not moved) — reclaiming disk is a follow-up operator decision.
- [x] **Endpoints** (gated by `storage.s3.enabled`):
  - `POST /api/admin/migrate-to-s3` → 202 + run snapshot. 409 if a run is already in progress.
  - `GET  /api/admin/migrate-to-s3/status` → current/last run snapshot.
  - `POST /api/admin/migrate-to-s3/cancel` → graceful stop at next row boundary.
- [x] Idempotency tests (`MigrationServiceTest`): re-upload skipped when key already in S3, no-op for already-migrated rows, throws on missing local file, concurrent start respects the singleton.

### Testing
- [ ] `FileStorageServiceIT` against a MinIO Testcontainer.
- [ ] e2e gallery + slideshow + public-token flows.
- [ ] Backwards-compat: rows with absolute paths still serve correctly during the migration window.

### Risk
MinIO single-replica is an SPOF for new writes. Acceptable for single-node K3s; underlying Longhorn PVC is durable.

---

## Gap 2b — Worker Deployment split (Phase 4)

Splits the single backend Deployment into two: an **api** pod that handles HTTP + serves bytes, and a **worker** pod that drains `processing_jobs`. Same JAR, same image, different `SPRING_PROFILES_ACTIVE`. Sharing surface is **DB + MinIO only** — no shared filesystem, no in-process handoff. Phase 3 (object storage) is the prerequisite that makes this physically separable.

### Phase 4-prep — decouple `FileStorageService` from `ThumbnailService`

The api pod must boot without pulling Vips/Heic/Ffmpeg beans. Today `FileStorageService` injects `ThumbnailService` purely to call predicates (`isImageFile`/`isVideoFile`/`isHeicFile`) and pure file-deletion helpers (`deleteThumbnails`/`deleteTranscodedVideo`). The remaining heavy uses are in admin endpoints (rotate, `generateMissingThumbnails`, `updateTranscodedVideoPaths`, `updateVideoThumbnailPaths`) which per **D17** stay non-functional for one release post-split.

- [x] New `MimeTypePredicates` static utility — `isImageFile`, `isHeicFile`, `isVideoFile`. No DI.
- [x] New `LocalFileCleanupService` `@Component` — `deleteThumbnails`, `deleteTranscodedVideo`. No Vips/Heic/Ffmpeg deps.
- [x] `FileStorageService.deleteFile` calls `LocalFileCleanupService` instead of `ThumbnailService`.
- [x] `ThumbnailService` predicate methods become thin forwarders to `MimeTypePredicates`; cleanup methods delegate to `LocalFileCleanupService`. Both kept for backwards compat during the transition; remove in R3.
- [x] All other callers (`FileProcessingService`, `SlideshowRecordingService`, etc.) keep using `thumbnailService.is*` until R3 cleanup; no churn now.

### Phase 4a — Profile-gate beans (mergeable, no behaviour change)

Both profiles still co-located in one Deployment. `SPRING_PROFILES_ACTIVE` defaults to `api,worker` so existing deploys keep working. New `Profiles` constants class for grep-ability.

- [x] **Worker-only** (`@Profile(Profiles.WORKER)`): `JobDispatcher`, `FileProcessingService`, `ThumbnailService`, `VipsThumbnailService`, `HeicConversionService`, `FfmpegService`, `JobLeaseService`.
- [x] **API-only** (`@Profile(Profiles.API)`, class-level per **D20**): all 18 controllers, `FileStorageService`, `SlideshowRecordingService`, `UploadBackpressureFilter`, `AsyncConfig`, `SecurityConfig`, `WebConfig`, `OpenApiConfig`, `MigrationService` / `RecordingMigrationService`, `VerificationService` / `RecordingVerificationService`, `EmailService`, `ApnsService`, `DeviceTokenService` (cron), `AlbumSubscriptionNotificationService` (cron), **`AlbumService` / `UserService` / `AlbumSubscriptionService`** (added during R2 pre-flight — each transitively depends on `FileStorageService` or `EmailService`). Last two crons **must** be api-gated or both pods double-fire.
- [x] **Both profiles** (no `@Profile`): all repositories, `ObjectStorageService` + `ObjectStorageConfig`, `BucketBootstrapper`, `JobEnqueueService`, `JobQueueDepthService`, `JobMetricsConfig`, `JobsProperties`, `AudioReencodingService` (per **D18**), shared "utility" services (`TagService`, `GallerySettingService`, etc.). Original plan listed `AlbumService` / `UserService` / `AlbumSubscriptionService` here; R2 pre-flight boot smoke caught that each injects an api-only bean (`FileStorageService` for `AlbumService`, `EmailService` for the other two) — they were promoted to API-only above.
- [x] `FileStorageService` switches `ThumbnailService` injection to `Optional<ThumbnailService>`; admin/rotate methods throw `IllegalStateException` when empty via a `requireThumbnailer()` helper (api pod, post-split). Per **D17** those endpoints are documented broken until Phase 4.5. **Also discovered**: `FileProcessingService` injection had the same shape (legacy `@Async` fallback path), so it's likewise wrapped in `Optional<>` with an `IllegalStateException` if `jobs.dispatcher.enabled=false` on api-only deploys. `AlbumService` gets `Optional<ThumbnailService>` for the same reason (video-date backfill skipped when absent).
- [x] `application.yml` defaults `spring.profiles.active=${SPRING_PROFILES_ACTIVE:api,worker}` so existing deploys, dev runs, and the unit-test suite keep loading both profiles. Per-pod env (`api` / `worker`) overrides at R2.
- [x] New `config/Profiles.java` constants class — `Profiles.API` / `Profiles.WORKER`, used in every `@Profile` annotation.
- [x] Bean-context tests: `WorkerProfileContextTest` + `ApiProfileContextTest` (under `test/.../profile/`) assert presence/absence of the gated beans. Gated by `-Drun.testcontainers=true` like `ProcessingJobLeaseTest` because they need a real MariaDB to satisfy `ddl-auto: validate` + Flyway.

### Phase 4b — Worker pod posture

- [x] Disable the main HTTP listener on the worker pod via `SERVER_PORT=-1` env var on `deployment-worker.yaml` (per **D19**). **Initially shipped as `application-worker.yml`, which broke the api pod** — Spring loads profile-specific YAMLs whenever the profile is *active*, not "active alone", so on the default `api,worker` deploy the override killed Tomcat 8080 there too (502 on `/api/auth/check`). Env var on the worker Deployment scopes the override correctly.
- [x] Confirmed `@EnableScheduling` is safe. The four `@Scheduled` beans split as: `JobDispatcher` (worker), `JobQueueDepthService` (both — refreshes a per-pod local cache, no side-effects), `DeviceTokenService` (api, cron), `AlbumSubscriptionNotificationService` (api, cron). No double-firing in any mode (api+worker, api-only, worker-only).
- [x] `FileStorageService.init()` `@PostConstruct` (creates `.multipart-tmp`) is api-only after gating — fine, worker doesn't need it. `FileProcessingService` continues to create `.processing-tmp/{id}` lazily.

### Phase 4c — Helm

- [x] `values.yaml`: new `worker.*` section parallel to `backend.*` (replicas: 0 default for R1; `worker.image.tag` falls through to `backend.image.tag` so a single release ships both pods at the same version). Backend gets two new keys: `backend.sprintProfilesActive` (empty default → `application.yml`'s `${SPRING_PROFILES_ACTIVE:api,worker}` applies, no behaviour change) and `backend.jobsDispatcherEnabled` (defaults `true`). **Slim `backend.*`** (`limits.memory: 1Gi`, JVM `-Xmx512m`) is deferred to the R2 release commit so R1 truly stays no-op.
- [x] `deployment-backend.yaml`: env `SPRING_PROFILES_ACTIVE` rendered only when non-empty; env `JOBS_DISPATCHER_ENABLED` always rendered (default `"true"`).
- [x] New `deployment-worker.yaml` (`{{ if .Values.worker.enabled }}`): same image/env block as backend, `SPRING_PROFILES_ACTIVE=worker`, **no Service / no Ingress**, ports 8081 only, `emptyDir` mount at `/app/uploads`, resources `limits: { cpu: 4000m, memory: 2Gi }` / `requests: { cpu: 500m, memory: 1Gi }`, JVM `-Xmx512m -XX:MaxRAMPercentage=35.0`, `replicas: {{ .Values.worker.replicas }}` (default 0 in R1 per **D22**).
- [x] New `service-worker-metrics.yaml`: clone of `service-backend-metrics.yaml` with worker selector + matching `prometheus.io/scrape` annotations.
- [x] `_helpers.tpl`: `photo-upload.worker.{fullname,labels,selectorLabels}` helpers (mirror backend).
- [x] `NOTES.txt`: added `PhotoUploadWorkerDown` rule (uses `kube_deployment_status_replicas_available == 0` so a 0-replica R1 manifest doesn't false-alert). `PhotoUploadJobsBacklog` description rewritten to cover both pre-split (in-pod dispatcher) and post-split (worker pod) semantics.
- [x] `helm template` + `helm lint` clean (pre-existing warnings about missing object-storage creds in defaults are unrelated).

### Phase 4d — Circuit breaker for "MinIO unreachable"

Fail-fast contract: 503 within ~50 ms, never hang Tomcat threads. Resilience4j (per **D16**).

- [x] Added `resilience4j-circuitbreaker` + `resilience4j-micrometer` (core libs, not the `-spring-boot3` starter — keeps us Spring Boot version-agnostic and skips AOP we don't need; the breaker is invoked explicitly from the service layer).
- [x] `ObjectStorageService` runs each SDK call through `breaker.executeSupplier(...)` / `executeRunnable(...)`. `CallNotPermittedException` (breaker OPEN) is translated to `MinioUnavailableException` → `GlobalExceptionHandler` 503 with `Retry-After: 30`. Wrapping at the service layer means **all callers** (api `FileStorageService.storeFile`, `ImageServeController` / `SlideshowRecordingController` reads, worker `FileProcessingService` download/put) get the breaker for free, without per-controller annotations.
- [x] SDK timeouts on the `S3Client`: `apiCallAttemptTimeout=2s`, `apiCallTimeout=5s` (defaults were unbounded → 90 s SDK retries pinned Tomcat threads on outage). Retry count not adjusted explicitly — `apiCallTimeout=5s` upper-bounds retries inside the budget; the breaker handles repeated-failure fast-fail.
- [x] `UploadBackpressureFilter` injects `Optional<CircuitBreaker> minioCircuitBreaker`; when state is `OPEN` it returns 503 + `Retry-After: 30` **before** parsing the multipart body — saves the staging cost during a MinIO outage.
- [x] Serve paths automatically covered by the service-layer wrap (`ImageServeController` and `SlideshowRecordingController` both call `ObjectStorageService.openStream(...)` which is breakered). No per-controller annotations needed.
- [x] `health/MinioHealthIndicator` flips readiness DOWN when the breaker is OPEN; HALF_OPEN reports UNKNOWN to avoid flapping during recovery; CLOSED is UP. K8s removes the api pod from the Service for the duration of an outage.
- [x] Worker-side automatically covered (same service-layer wrap). When the worker can't reach MinIO mid-job, the SDK timeout raises through the breaker → `FileProcessingService` catches → `markFailedOrDeadLetter` runs the existing failure path.
- [x] Resilience4j defaults configured programmatically in `ResilienceConfig` (not `application.yml` — since we skipped the Spring Boot starter, the YAML auto-config keys aren't wired). Values match the plan: `failureRateThreshold=50`, `slowCallRateThreshold=100`, `slowCallDurationThreshold=3s`, `slidingWindowSize=20`, `minimumNumberOfCalls=5`, `waitDurationInOpenState=10s`, `permittedNumberOfCallsInHalfOpenState=2`, `automaticTransitionFromOpenToHalfOpenEnabled=true`.
- [x] `TaggedCircuitBreakerMetrics` bound to Micrometer so `resilience4j_circuitbreaker_state{name="minio",...}` etc. surface on `/actuator/prometheus` for both api and worker pods.

### Phase 4e — Roll-out

**R1 — manifests only.** [x] **Landed 2026-04-29 ~18:00 (Helm rev 19).** `worker.enabled: true`, `worker.replicas: 0`, `backend.sprintProfilesActive: ""` (falls through to `application.yml` default `api,worker`). Manifest validation only; behaviour identical to pre-split.

**R2 — worker takes processing.** [x] **Landed 2026-04-29 ~20:43 (Helm rev 20, image `picz2-be:1.0.33`); rev 21 corrected `jobsDispatcherEnabled` after first upload failed.** `worker.replicas: 1`, `backend.sprintProfilesActive: "api"` (drops `worker`), **`backend.jobsDispatcherEnabled: true`** (see correction below). `worker.sprintProfilesActive: "worker"`, `worker.jobsDispatcherEnabled: true`. Backend slimmed: `limits.memory 3Gi → 1Gi`, `requests.memory 1536Mi → 768Mi`, `-Xmx1024m → 512m`, `-Xms512m → 256m`. `SELECT … FOR UPDATE SKIP LOCKED` makes any rolling-update overlap safe. Soak 24 h before R3.

  **Earlier-plan correction.** This document originally said R2 sets `backend.jobsDispatcherEnabled: false` "→ api pod enqueues but never drains" with a "subtle invariant" that the property only controls the enqueue contract. **That was wrong.** The actual code in `FileStorageService.storeFile` reads:
  - `dispatcherEnabled=true` → row inserted as `QUEUED` + `processing_jobs` row written via `JobEnqueueService`. No legacy call.
  - `dispatcherEnabled=false` → row inserted as `INGESTED` + **no `processing_jobs` row** + falls through to `legacy.processFileAsync(...)`. Requires `FileProcessingService` (worker-only). On the api pod that bean is absent → `IllegalStateException`. The first upload after R2 hit exactly this and stranded asset id 4992 as `INGESTED` with no job. R2 was corrected by setting the api pod's flag to `true`; drain remains worker-only because `JobDispatcher` is `@Profile(WORKER)` regardless of the property. Asset 4992 was manually re-enqueued (job 7) and processed cleanly.

  **R2 pre-flight findings** (caught by local boot smoke when Testcontainers couldn't run on Docker Desktop):
  - `AlbumService`, `UserService`, `AlbumSubscriptionService` were listed as "shared" in Phase 4a but each transitively depended on an api-only bean (`FileStorageService` / `EmailService`). Worker boot crashed `UnsatisfiedDependencyException`. Fixed by adding `@Profile(API)` (committed 24d5e43). Phase 4a list updated above.
  - Helm template bug: `value: {{ .Values.backend.jobsDispatcherEnabled | default true | quote }}` rendered `false` as `"true"` because Go-template `default` treats boolean `false` as falsy. Same bug in worker template. Patched both — `default` removed; `values.yaml` is the source of truth. (Fixing this template bug is what unmasked the dispatcher-flag bug above — pre-fix, the api pod was always reading `true` regardless of values.)

**R3 — cleanup.** [x] **Landed 2026-04-30** after a clean R2 soak (DB read: 2446/2446 file_metadata DONE, 38/38 processing_jobs DONE, 0 DEAD_LETTER). Removed: `FileProcessingService.processFileAsync` + the `@Async` annotation, `AsyncConfig` (whole file deleted; no remaining `@Async` users), `INGESTED` from `ProcessingStatus` (DB column + V30 default kept — every insert sets the value explicitly), `Optional<ThumbnailService>` and `Optional<FileProcessingService>` shims on `FileStorageService` plus the legacy fallback branch in `storeFile`, `requireThumbnailer()`, the three deferred admin methods (`generateMissingThumbnails` / `updateTranscodedVideoPaths` / `updateVideoThumbnailPaths`) and their `AdminController` endpoints, the `is*`/`deleteThumbnails`/`deleteTranscodedVideo` forwarders on `ThumbnailService` (callers updated to use `MimeTypePredicates` / `LocalFileCleanupService` directly), the `Optional<ThumbnailService>` shim + video-date backfill loop on `AlbumService`, and the `UploadBackpressureFilter` executor-fallback branch (and its `ThreadPoolTaskExecutor` injection). Also removed the now-vestigial `jobs.dispatcher.enabled` flag end-to-end (`JobsProperties.Dispatcher`, `application.yml`, `values.yaml`, `deployment-backend.yaml`, `deployment-worker.yaml`, the early-returns in `JobDispatcher` / `JobQueueDepthService` / `UploadBackpressureFilter`) — there's no fallback to gate. iOS `AssetProcessingStatus` and frontend `ProcessingStatus` types likewise dropped `INGESTED`. Test suite parity: 6 failures on the changed branch, identical to master HEAD (verified via stash + retry) — R3 introduces zero regressions.

### Testing

- [x] `WorkerProfileContextTest` / `ApiProfileContextTest` assert bean presence per profile (gated by `-Drun.testcontainers=true`, same as `ProcessingJobLeaseTest`).
- [ ] Cross-profile JVM Testcontainers: spin two `SpringApplicationBuilder().profiles(...).run()` contexts against one MariaDB. Api context inserts `FileMetadata` + `ProcessingJob`; worker context picks it up; both rows reach `DONE`.
- [ ] Chaos: `kubectl scale deploy/worker --replicas=0` mid-job → lease expires → resume on restart. `--replicas=2` → no asset has `processing_attempts > 1`.
- [ ] CB chaos: `kubectl scale deploy/minio -n minio --replicas=0` → upload returns 503 within ~50 ms once breaker is OPEN; restore → recovery within ~20 s.

### Risk / rollback

R2 rollback: `backend.sprintProfilesActive=""` (empty → application.yml default `api,worker`), `backend.jobsDispatcherEnabled=true` (was already true post-fix), `worker.replicas=0`. Coexistence is safe at every step because of `SKIP LOCKED`. Per-pod `emptyDir` for processing scratch is the same restart-loses-in-flight property the PVC had post-Gap 8 — no new exposure.

### Follow-ups (not blocking the split)

- [x] **Phase 4.5**: rewrite admin / rotate endpoints as worker-side jobs (per **D17**). Closed 2026-05-03 — REGEN_THUMBNAILS landed; the two video-path admin methods are deliberately not rebuilt (PVC unmounted, no use case, see notes below).
  - [x] **Rotate-left**: `processing_jobs.job_type` column added (V32, **D23**); `JobType.{PROCESS,ROTATE_LEFT}` discriminates dispatch; `FileProcessingService.rotateAndReprocess` runs on worker; `FileStorageService.rotateImageLeft` (api) flips `processing_status=QUEUED` + enqueues `ROTATE_LEFT` in same TX; `FileController` returns 202; `GalleryView.vue` polls `/api/assets/{id}/status` (1 s, 60 s cap) before reloading. Note: `IllegalStateException` was never mapped to 503 in `GlobalExceptionHandler` so the broken pre-fix endpoint actually returned 500 — moot now that the path works.
    - **Post-deploy follow-ups landed 2026-04-29:**
      - `oglimmer.sh` only restarted `photo-upload-backend` on `-s`, not `photo-upload-worker`. With `:latest` + `imagePullPolicy=Always`, this leaves the worker on its previously-cached image and silently runs old `JobDispatcher` code against the new schema — ROTATE_LEFT jobs get dispatched as PROCESS, no rotation, no error. Patched: script now restarts both whenever `BUILD_BACKEND=true`, and gained a `--worker-deploy` flag + `WORKER_DEPLOYMENT` env override mirroring the backend pattern.
      - Gallery `<img>` kept showing the pre-rotate bytes until a full page refresh. Root cause: `:key="file.id"` made Vue reuse the same `GalleryItem` instance across `loadAlbumFiles`, and the browser stubbornly served the previously-loaded image despite the new `publicToken` URL. Fix: `:key="\`${file.id}:${file.publicToken}\`"` forces a remount with a fresh `<img>` element when the token changes (which only happens on rotate — uploads/reorder/status polls keep the key stable).
      - The "Processing…" spinner placeholder was missing during rotate because the rotate handler polled a local variable instead of mutating the file's `processingStatus`. Fix: in `handleRotateImage`, set `target.processingStatus = 'QUEUED'` immediately after the 202, and update it from each poll tick. `GalleryItem.thumbnailReady` flips false → spinner shows for the duration of the worker job, identical UX to a fresh upload.
  - [x] **`generateMissingThumbnails`** rewritten as a worker job (2026-05-03). New `JobType.REGEN_THUMBNAILS`; `FileProcessingService.regenerateThumbnails` reuses the rotate-side `pickRotationSource` fallback chain (original → large → medium → thumb), so it works on retention-purged assets too. Identical lease/retry/dead-letter machinery as ROTATE_LEFT. New `FileMetadataRepository.findMissingThumbnailIds(maxRows)` filters image-typed DONE rows missing one of the three derivative paths AND with no active job (idempotent re-runs). New `FileStorageService.enqueueRegenForMissingThumbnails(maxRows)` walks the IDs in one TX, flipping each row to QUEUED + enqueueing the job. `POST /api/admin/regen-missing-thumbnails?maxRows=500` (capped at 5000) returns `{success, message, stats:{enqueued, maxRows}}`. Caller pages by re-invoking until `enqueued == 0`.
  - **`updateTranscodedVideoPaths`, `updateVideoThumbnailPaths`** — won't-build (decision 2026-05-03). Both originally scanned the local PVC to repair drifted DB paths. The PVC is unmounted post-Gap 8 and all paths are deterministic S3 keys derived from `assetId`, so they cannot drift in the new architecture. Failures during PROCESS surface via DEAD_LETTER + the new REGEN_THUMBNAILS sweep above. If a future use case re-emerges (e.g. bulk re-transcode after an ffmpeg upgrade), add a `REGEN_VIDEO` JobType using the same pattern as REGEN_THUMBNAILS.
  - [x] `AlbumService` video-date backfill — removed in R3 (the loop was an api-side ffprobe call, impossible after the worker split). If we want creation-time backfill again, enqueue a `BACKFILL_VIDEO_DATE` worker job per row; for now ordering by upload date when EXIF is absent is the documented behaviour.
  - [x] **Capture-date normalisation** (2026-08-17, **D32**) — supersedes the `BACKFILL_VIDEO_DATE` sketch above. `exif_date_time_original` now holds a true instant for both media types: new `CaptureDateExtractor` (worker) applies `OffsetTimeOriginal` or `capture-date.fallback-zone` to the zone-less EXIF wall clock instead of letting metadata-extractor assume GMT, and `FfmpegService.extractVideoCreationDate` prefers `com.apple.quicktime.creationdate` over the mvhd `creation_time` and no longer drops the value when the offset is written colon-less (`+0200`), which `Instant.parse` rejects. V34 adds `exif_date_source` (`CaptureDateSource` enum) so legacy rows stay identifiable. Backfill: `JobType.EXTRACT_CAPTURE_DATE` → `FileProcessingService.reextractCaptureDate` (metadata only — no transcode, no derivative regeneration), enqueued in batches by `POST /api/admin/reextract-capture-dates?maxRows=500` over `findStaleCaptureDateIds`. Self-shrinking set, so page until `enqueued == 0`. Retention-purged rows are excluded — the capture time exists only in the original. **Not yet run against prod.**
  - [x] **Capture-location extraction + map filter** (2026-08-20, **D34**) — same backfill shape as capture dates, one rung down the pipeline. `GpsExtractor` (worker) reads the EXIF GPS IFD via metadata-extractor's `getGeoLocation()` (which already applies the N/S, E/W reference tags — reconstructing the sign by hand mirrors photos into the wrong hemisphere) and `FfmpegService.extractVideoLocation` parses the ISO 6709 QuickTime/`location` tag. `GpsCoordinates.of` rejects out-of-range values and the 0/0 null-island sentinel cameras write when the GPS chip has no fix — kept, those would drop a pin in the Gulf of Guinea for every unlocated photo. V36 adds `gps_latitude`, `gps_longitude`, `gps_source` (+ `idx_album_gps`). Backfill: `JobType.EXTRACT_GPS` → `FileProcessingService.reextractGps` (metadata only), enqueued by `POST /api/admin/extract-gps?maxRows=500` over `findMissingGpsIds`. Self-shrinking set, so page until `enqueued == 0`. **Retention-purged rows are permanently unrecoverable here** — coordinates exist only in the original and derivatives carry none, so anything already swept by retention will never appear on the map. Run against prod 2026-08-20.
- [ ] **`/actuator/prometheus` returns 401** (pre-existing, surfaced during R2 verification). API pod's `SecurityConfig` only permits `/actuator/health/**` and `/actuator/info`; worker has no `SecurityConfig` so falls to Spring Security secure-by-default. Means the `PhotoUploadJobsBacklog` / `PhotoUploadJobsDeadLetter` alerts in `NOTES.txt` would never fire because the scrape itself fails. Permit `/actuator/prometheus` (cluster-network only) on both pods, or add a worker-side `WorkerSecurityConfig` (`@Profile(WORKER)`) that allows the actuator namespace.

---

## Gap 3 — TUS resumable uploads (Phase 5)

### Architecture (D7, accepted)

`tusd` Deployment writing **directly to MinIO** via the S3 backend. tusd never hits the JVM for chunk PATCHes — only thin hook callbacks on `pre-create` and `post-finish` reach the api pod. Worker pod is unchanged: it drains `processing_jobs` whether the row arrived via multipart or TUS.

```
[client] -- POST /files/ ------> [tusd] -- pre-create  hook --> [api]
[client] -- PATCH /files/{id} -> [tusd] -- S3 multipart -----> [MinIO]
                                 [tusd] -- post-finish hook --> [api] -> insert file_metadata + processing_jobs
[worker] <-- SKIP LOCKED lease ---------------------------- [processing_jobs]
```

Phase 6 (retention) landed before Phase 5, breaking the original sequence diagram. That is fine: retention's eligibility query (`file_path LIKE 'originals/%'`) does not match the `tus-uploads/` prefix, so the new namespace is out of scope. Stale TUS uploads are swept by tusd's own `-expire-after=168h` plus a MinIO bucket-lifecycle abort-rule.

### Phase 5a — tusd in cluster (manifests only, no client traffic)

- [x] Helm: `deployment-tusd.yaml` (image `tusproject/tusd:v2`, single replica per **D29**). Args:
  ```
  -s3-bucket=$(STORAGE_S3_BUCKET)
  -s3-endpoint=$(STORAGE_S3_ENDPOINT)
  -s3-object-prefix=tus-uploads/
  -s3-disable-content-hashes
  -hooks-http=http://photo-upload-backend:8080/api/tus/hooks/$(TUS_HOOK_SECRET)
  -hooks-http-forward-headers=Authorization
  -hooks-enabled-events=pre-create,post-finish,post-terminate
  -behind-proxy
  -base-path=/files/
  ```
- [x] `service-tusd.yaml` ClusterIP:1080.
- [x] `_helpers.tpl`: `photo-upload.tusd.{fullname,labels,selectorLabels}` mirroring backend / worker.
- [x] `secret.yaml`: new `tus.hookSecret` entry; required when `tus.enabled` (operator generates with `openssl rand -hex 32`); injected as env to both tusd and api Deployments.
- [x] Ingress: route `/files/*` to tusd Service. `/api/*` continues to api Deployment. `/api/tus/hooks/{secret}` is **not** exposed externally. Conditional skip in `ingress.yaml` ensures `tus.enabled=false` doesn't render a /files entry pointing at a missing Service.
- [x] MinIO bucket lifecycle: `mc ilm rule add --prefix tus-uploads/ --expiry-days 7` documented in `NOTES.txt` — operator applies once per environment, like the existing prometheus alert rules. This is platform-side because the bucket lives in the platform-managed `minio` release. **Required, not optional**: tusd 2.x dropped its in-process `-expire-after` flag (originally in the plan but discovered missing during R1 deploy — the pod CrashLoopBackOff'd until we removed the flag), so the bucket lifecycle is the only GC mechanism for stale `tus-uploads/{uuid}` objects.
- [x] `helm template` + `helm lint` clean. Verified both `tus.enabled=true` (renders Deployment + Service + Ingress route + secret + backend env vars) and `tus.enabled=false` (omits all of the above) paths.

  **Implementation note** — the original recommendation in D27 was HMAC-signed hook bodies. tusd 2.x cannot sign hook bodies, only forward client request headers, so HMAC was infeasible without a custom shim. Substituted with a path-secret in the hook URL (`/api/tus/hooks/$(TUS_HOOK_SECRET)`) validated with constant-time compare. Functionally equivalent defence-in-depth.

  **Integer rendering bug caught during validation** — `maxSize: 524288000` rendered as `5.24288e+08` (Helm/Go template treats YAML numeric as float64). Fixed in both `deployment-tusd.yaml` arg (`-max-size={{ .Values.tus.maxSize | int64 }}`) and the backend `TUS_MAX_SIZE` env (`int64 | quote`). Same bug class as the earlier R2-pre-flight `default true | quote` rendering issue.

### Phase 5b — backend hooks (still no client traffic)

- [x] Added `ObjectStorageService.copy(srcKey, dstKey, contentType)` (server-side `CopyObjectRequest` with `MetadataDirective.REPLACE` for content-type override; no JVM bytes; breaker-wrapped).
- [x] `TusProperties` (`@ConfigurationProperties("tus")`): `enabled` (default false), `advertised` (default false), `endpoint` (default `/files/`), `maxSize`, `hookSecret`, `version`.
- [x] `TusHookController.POST /api/tus/hooks/{secret}` (`@Profile(API)` + `@ConditionalOnProperty(tus.enabled)`):
  - Path-secret validated with `MessageDigest.isEqual` (constant-time) before any further work; mismatch → 404 (matches Spring's "unmapped URL" response shape so a probe can't tell whether the endpoint exists).
  - `pre-create` handler: parse `Upload-Metadata.auth` as `email:password`, validate via the existing `AuthenticationManager` (HTTP Basic, same path the multipart endpoint uses); reject 409 if `contentId` already in `file_metadata` (per **D30**); reject 503 with `Retry-After: 30` if `JobQueueDepthService.depth > threshold` (per **D28**); 200 otherwise.
  - `post-finish` handler: re-validates auth (Authorization header + `auth` metadata both flow on every PATCH); short-circuits 200 when a row already exists for the contentId (idempotent retry); otherwise calls `FileStorageService.registerTusUpload(...)` which COPIES, inserts the row + enqueues a PROCESS job in one TX, and deletes the tus-uploads/{uuid}+`.info` objects best-effort.
  - `post-terminate` handler: logs and 200s. tusd already cleaned up the S3 object.
  - Crucially: post-finish never returns 5xx, even on internal failure. Returning 5xx would loop tusd retries against an already-finalised upload that can't be undone; orphan-detection (follow-up) mops up any stranded `originals/...` objects.
- [x] `CapabilitiesController.GET /api/capabilities` (`@Profile(API)`) returns `{ tus: { enabled, endpoint, version, maxSize }, multipart: { enabled, endpoint } }`. `tus.enabled` here is *advertised* (the user-facing flag); pin to `false` in R1, flip to `true` in R2.
- [x] `SecurityConfig`: permit `/api/tus/hooks/**` (path-secret is the auth) and `GET /api/capabilities`.
- [x] `application.yml`: `tus.enabled`, `tus.advertised`, `tus.endpoint`, `tus.max-size`, `tus.hook-secret`, `tus.version`. Helm wires the secret-backed `TUS_HOOK_SECRET` from the `Secret`.
- [x] Added `FileStorageService.registerTusUpload(...)` — TUS-side parallel to `storeFile`. Same TX boundary (row + enqueue), same `originals/{stored_filename}` invariant, but skips checksum-based dedupe (would require downloading from S3, defeats the point of TUS); contentId dedupe in pre-create is the only de-dupe path on the TUS side. Documented as a known limitation in V1.
- [x] Unit tests: `TusHookServiceTest` — 10 tests covering pre-create missing-auth / bad-credentials / backpressure / duplicate-contentId / happy-path; post-finish register / idempotent-retry / swallowed-auth-failure / swallowed-register-failure; post-terminate ack. `TusHookControllerTest` — 8 tests covering path-secret guard + dispatch + 200-with-JSON envelope. All pass. Full server suite shows 6 pre-existing failures (`TagServiceTest`, `JobLeaseServiceTest`×4, `FileProcessingServiceStatusTest`) — same count as documented in the R3 cleanup section, no Phase 5 regressions.

  **Post-R1-deploy bug fix #2 (2026-05-01)** — first real upload via `tus-js-client` from the web frontend hit `HTTP/2 500: ERR_INTERNAL_SERVER_ERROR: failed to parse hook response: unexpected end of JSON input`. Root cause: tusd 2.x **requires** the hook response body to be a JSON-shaped {@code HookResponse} envelope (`{"HTTPResponse":{...},"RejectUpload":bool}`), and uses the inner `HTTPResponse.StatusCode` to decide what to surface to the actual client. The original implementation returned `ResponseEntity.ok().build()` (HTTP 200 with empty body) and used 4xx/5xx HTTP statuses for rejections — both wrong. Fixed by introducing a new `TusHookResponse` DTO (records mapping to PascalCase via `@JsonProperty`), refactoring `TusHookService` methods to return `TusHookResponse` instead of `ResponseEntity<Object>`, and having `TusHookController` always wrap in `ResponseEntity.ok(...)`. Path-secret-mismatch is the one exception that still returns bare HTTP 404 — tusd never calls with a wrong secret in normal operation, and 404 keeps the path's existence unobservable to probes.

  **Post-R1-deploy bug fix #3 (2026-05-01)** — second upload attempt got past the hook-envelope fix and reached `post-finish`, then the `objectStorage.copy(tusKey, ...)` raised `NoSuchKeyException`. Root cause: tusd's S3 store synthesises {@code Upload.ID} as `<objectName>+<base64-multipartUploadId>`, where only the part before the `+` is the actual S3 object key. Our code reconstructed the key naively as `tus-uploads/{Upload.ID}` — pointing at a path that never exists. Fixed by adding a `Storage` field (`Map<String,String>`) to `TusHookRequest.TusUpload` and reading the authoritative `Storage.Key` set by tusd. Falls back to splitting `Upload.ID` on `+` if a future tusd version drops `Storage`. Applies to both `post-finish` (COPY+DELETE source) and `post-terminate` (logging).

  **Post-R1-deploy bug fix #4 (2026-05-01) — TUS post-finish-vs-PATCH-204 race.** With all the server-side fixes in, uploads landed cleanly but the new image didn't show in the gallery without a hard refresh. tusd's logs revealed the cause: contrary to its docs claim that "the client will not get a response until after the hook has been completed", tusd 2.4.0 sends the PATCH 204 *before* invoking `post-finish`:

  ```
  21:38:43.652206 - ResponseOutgoing PATCH status=204    ← client gets 204 here
  21:38:43.654200 - HookInvocationStart type=post-finish ← THEN post-finish starts
  21:38:43.869412 - HookInvocationFinish type=post-finish ← row finally committed (217ms later)
  ```

  So `tus-js-client.onSuccess` fires, `await loadAlbumFiles` runs, and `/api/files` is queried ~200ms before the `file_metadata` row exists. Result: the response correctly returns the *old* file list; the gallery doesn't re-render because the new file isn't there. After ~3 minutes, a hard refresh sees the row and renders it.

  Fixed in `GalleryView.vue#handleFileUpload`: snapshot the file-list length before the upload loop, then poll `loadAlbumFiles` until `files.value.length >= initialCount + successCount` or a 3 s deadline, with 300 ms gaps between polls. First load happens immediately; the loop only iterates if the row hasn't shown up yet. Multipart is harmless to this change since its row commits before the response.

  Three layers of timing-related lessons here, all because the docs disagreed with the actual behaviour:
  1. tusd's `Upload.ID` is `<objectName>+<multipartUploadId>` — not a key (#3).
  2. tusd's hook response *body* must be a JSON envelope, not a plain HTTP status (#2).
  3. tusd's `post-finish` is **not** synchronous despite docs — the PATCH 204 fires before the hook starts.

### Phase 5c — iOS client (gated, default off)

**Status: shipped end-to-end and verified on device (2026-05-02).** TUS uploads from iOS land cleanly when `Settings.useTus=true`; multipart path is unaffected when off. Two pre-existing iOS issues surfaced and were fixed during the first Xcode build:

1. `ProcessingStatusPoller` line 78 still referenced `AssetProcessingStatus.ingested` — the enum case had been removed during Phase 4 R3 cleanup but the iOS target hadn't been built since, so the dangling reference went unnoticed. Removed.
2. `SyncCoordinator.syncTargetAlbumFromServer` async-clear race (when server returns null target album): completion fired before the `DispatchQueue.main.async` clear, so the caller's `guard settings.selectedAlbumName != nil` saw the stale non-nil value and proceeded with a doomed scan. Moved `completion(true)` inside the async block so the caller observes the post-clear state. Pre-existing, only fires on the rare server-clears-target-album path; no data loss in either case.

Also surfaced (not fixed in this commit): the user's `User.defaultAlbumId` was null, so iOS multipart uploads were 400ing because they don't include `albumId` in the form body and the server falls back to `currentUser.getDefaultAlbumId()`. Resolved out-of-band by `PUT /api/settings/target-album`. Operational, not a code bug.

- [x] ~~`Settings.useTus: Bool` — toggle in `SyncOptionsView` under an "Advanced" section~~ **Removed 2026-08-28.** The toggle was a second gate on a decision `/api/capabilities` already makes, on a server we control; its only reachable effect was letting a user opt out of resumable uploads. Setting, UserDefaults key and "Advanced" section all deleted.
- [x] `Models/Capabilities.swift` — Decodable matching the api response shape.
- [x] `APIClient.fetchCapabilities(completion:)` — unauthenticated GET, returns the same Decodable. Caller responsible for caching.
- [x] `TusUploader.swift` — singleton with its own background URLSession (identifier `com.oglimmer.photosync.tus`), drop-in shape match to `Uploader` (`configureSession`, `queueUpload`, `onTaskFinished`, `getActiveUploadAssetIds`). V1 sends a single PATCH per upload (the entire file from offset 0); the URLSession's built-in retry handles network blips. Cross-launch resume via `HEAD /files/{id}` is documented as deferred V2 work — needs Xcode + device verification.
- [x] `APIClient.tusEndpointURL()` and `APIClient.tusUploadMetadata(...)` extension helpers — TUS spec-compliant `Upload-Metadata` (comma-separated key + base64 value pairs); auth piggy-backs on the existing HTTP Basic credentials.
- [x] `SyncCoordinator` integration (2026-05-02) — picks `TusUploader.shared` vs `Uploader.shared` based on cached `/api/capabilities` (× `Settings.useTus` until that flag was removed on 2026-08-28), decided once per drainQueue batch (not per asset, to avoid a half-multipart-half-TUS batch if a capability flip races with the loop). Adapts `TusUploader.UploadOutcome` to `Uploader.UploadOutcome` in `init()` so `handleUploadFinished` stays path-agnostic. `start()` now configureSessions both uploaders, merges their `getActiveUploadAssetIds()` for stale-cleanup, and kicks off a non-blocking capabilities fetch (1 h TTL). Capabilities-fetch failure or pending state silently falls back to multipart.
- [x] **Status polling for TUS uploads** (2026-05-02) — fixed via a new `GET /api/assets/by-content?albumId=X&contentId=Y` endpoint that returns the same `AssetProcessingStatusResponse` shape as `/api/assets/{id}/status`. iOS `APIClient.lookupAssetByContentId` is called from `SyncCoordinator`'s TUS adapter when the upload completes with `serverAssetId=nil`; retries up to 4× with exponential backoff (200/400/800/1600ms) to ride out the post-finish hook race window (~200ms typical, 1s worst case observed in production). On success, normal `ProcessingStatusPoller` polling kicks in; on persistent failure (row never appears, hook crashed), nil is forwarded — upload is still recorded as success, only the FAILED/DEAD_LETTER notifications are unavailable for that asset.

### Phase 5d — Roll-out

- [x] **R1 — manifests only** (2026-05-01). tusd Deployment, hook secret, Ingress route. Four post-deploy tusd-protocol bugs found and fixed (see fix #1–#4 in Phase 5b notes). Verified `OPTIONS /files/` and hook round-trips end-to-end.
- [x] **R2 — flip capabilities** (2026-05-01). `tus.advertised=true`, web frontend uses TUS automatically; multipart kept as fallback. Verified with real photo upload through `tus-js-client`.
- [x] **R3 — all clients TUS by default** (2026-05-02). iOS `Settings.useTus` default flipped from `false` to `true` (init + `clear()`). Web has been on TUS since R2 (capability-driven, no per-user flag). Existing iOS users who explicitly toggled the switch off kept their preference until 2026-08-28, when the flag was removed outright and iOS became capability-driven like the web.
- [ ] **R4 — deprecate multipart.** Earliest 3 months after R3 (2026-08-02). `/api/upload` returns 410 Gone pointing at `/api/capabilities`. iOS minimum supported version forced upward.

### Testing

- [ ] **Unit**: hook-path-secret verification, pre-create dedupe, pre-create backpressure, post-finish row insert.
- [ ] **Integration** (Testcontainers MariaDB + MinIO + tusd): full round-trip — `POST /files/` → PATCH chunks → assert `file_metadata` + `processing_jobs` rows match the multipart path.
- [ ] **Resume**: PATCH 1 chunk, kill the connection, `HEAD /files/{id}` reports correct offset, resume completes, post-finish fires exactly once.
- [ ] **Pod restart**: `kubectl delete pod -l app=tusd` mid-upload. tusd persists state in S3, client resume succeeds after restart.
- [ ] **Backpressure**: pin queue depth above threshold via SQL → `POST /files/` returns 503 + `Retry-After`.
- [ ] **Duplicate**: same `contentId` twice → second `POST /files/` returns 409.
- [ ] **iOS chaos**: airplane-mode toggle mid-PATCH, force-quit + relaunch mid-PATCH, OS reboot mid-PATCH.
- [ ] **End-to-end gallery**: TUS-uploaded asset appears in gallery, derivatives render, public token works, rotate works.

### Risk / rollback

- **Stale TUS uploads**: client uploads but never completes → `tus-uploads/{uuid}` lingers. The MinIO bucket-lifecycle abort-rule is the **only** GC mechanism — tusd 2.x removed its in-process `-expire-after` flag (we discovered this during R1 deploy when the tusd pod CrashLoopBackOff'd on startup). Without the lifecycle rule applied, abandoned uploads accumulate indefinitely. Phase 6 retention sweep is unaffected — its WHERE clause matches `originals/%` only, so the `tus-uploads/` prefix is out of scope.
- **post-finish hook fails after S3 COPY succeeds**: row never written, but the `originals/{stored_filename}` object exists in MinIO. Recovery: orphan-detection job (compare `originals/*` listing against `file_metadata.file_path`) — deferred to follow-up; manual cleanup acceptable in early rollout.
- **Hook secret leak**: rotate via Helm secret + tusd Deployment restart + api pod restart. No long-lived state.
- **R2 rollback**: `tus.enabled=false` in api config. Existing in-flight TUS uploads on iOS will fail; the next `/api/capabilities` poll switches them back to multipart for the next attempt.
- **R4 rollback**: revert the `/api/upload` 410 commit. The multipart code path stayed in place; re-enabling is a config change.

### Follow-ups (not blocking ship)

- [x] **Orphan-detection job for `originals/` keys without `file_metadata` rows** (post-finish hook crash recovery, also covers multipart-PUT-then-TX-fail). Landed 2026-05-03 as a third pass in `RetentionService.runOriginalsOrphanCleanup`. Set-diffs `objectStorage.listKeysOlderThan("originals/", now − retention.orphan-grace-hours)` against `FileMetadataRepository.findAllOriginalsKeys()` (projection-only, no entity hydration). Default grace `24h` — wide enough that no in-flight upload can be misclassified (post-finish hook race is sub-second, ~1s observed worst-case), narrow enough that real orphans don't sit around for a week. Same `dryRun` / `maxRowsPerRun` knobs as the originals + TUS sweeps; `RetentionRunner` sums all three failure counts into the exit code. 7 unit tests in `RetentionServiceOrphanCleanupTest`; zero regressions in the existing TUS test suite.
- [ ] tusd Prometheus metrics scrape (`/metrics` endpoint native to tusd; add `prometheus.io/scrape` annotations to the tusd Service).
- [ ] Web frontend TUS support (`tus-js-client`). Currently web uses the multipart path; lower priority because web uploads are smaller.
- [ ] NetworkPolicy restricting `/api/tus/hooks/**` ingress to the tusd Pod's identity (defence-in-depth on top of D27 path-secret).
- [x] **Stale TUS upload GC** — discovered during R1 deploy: tusd 2.x removed `-expire-after`, and the platform-side MinIO is single-drive (no lifecycle API). Solved by extending `RetentionService` with a `runTusCleanup()` second pass that lists `tus-uploads/` and deletes objects older than `retention.tus-upload-days` (default 7). Same nightly CronJob, same dry-run/max-rows safety properties. New `ObjectStorageService.listKeysOlderThan(prefix, cutoff)` helper. `RetentionRunner` calls both passes and sums failure counts into the exit code. 6 unit tests added (`RetentionServiceTusCleanupTest`); zero regressions.

---

## Gap 4-finish — Retention CronJob (Phase 6)

- [x] `cronjob-retention.yaml`: nightly at `03:17`, `concurrencyPolicy: Forbid`, `restartPolicy: Never`, `backoffLimit: 0`. Runs the same backend image with `SPRING_PROFILES_ACTIVE=retention`. `application-retention.yml` strips Tomcat (`spring.main.web-application-type=none`) and the management port; `RetentionRunner.run()` calls `SpringApplication.exit(...)` so K8s reports Job/Completed.
- [x] V33 migration drops the `NOT NULL` on `file_metadata.file_path`; `FileMetadata.filePath` entity field updated to match.
- [x] `RetentionService` (under `@Profile(Profiles.RETENTION)`) finds rows where `processing_status='DONE'` AND `uploaded_at < NOW() - retention.original-days` AND `file_path LIKE 'originals/%'` AND `thumbnail_path IS NOT NULL` (defensive sanity check that *some* derivative exists) → S3 DELETE original → null `file_path` in its own per-row TX. `RetentionProperties` exposes `retention.original-days` (default 7), `retention.max-rows-per-run` (default 5000, prevents runaway sweep on misconfig), `retention.dry-run` (was `true` in chart values for first-run safety; **flipped to `false` on 2026-05-03** after the 2026-05-02 nightly logged eligible=2408 / 0 failures cleanly).
- [x] `RetentionRunner` (one-shot `CommandLineRunner`) wraps `RetentionService.run()` + `runTusCleanup()` + `runOriginalsOrphanCleanup()` and exits with `min(originals.failed + tus.failed + orphans.failed, 125)` so a partial-failure run shows on the CronJob's last-failed status without masking it as success.
- [x] **Orphan-detection third pass** (added 2026-05-03 — see Gap 3 Follow-ups). `RetentionService.runOriginalsOrphanCleanup` set-diffs S3 `originals/` listing (older than `retention.orphan-grace-hours`, default 24h) against `FileMetadataRepository.findAllOriginalsKeys()` to delete keys with no live row. Same `dryRun` / `maxRowsPerRun` knobs. `RETENTION_ORPHAN_GRACE_HOURS` env added to `cronjob-retention.yaml`.
- [x] File-serve adjustment: `FileStorageService.getFileServeInfoByPublicToken` falls back to the largest available derivative (`transcoded` for video > `large` > `medium` > `thumbnail`) when `filePath` is NULL and no specific size was asked for. Explicit `?size=original` on a purged asset throws the new `ResourceGoneException`, mapped to 410 by `GlobalExceptionHandler`.
- [x] **Rotation works after purge** (revised 2026-05-03). Originally `FileStorageService.rotateImageLeft` threw 410 when `filePath` was NULL and `GalleryItem.vue` hid the rotate button via `originalAvailable: boolean`. Walked back: rotate output is bounded by LARGE=2400px regardless of source resolution, so the worker can fall back to the largest available derivative (`large` → `medium` → `thumb`) and produce pixel-equivalent output to what the user already gets post-purge. `FileProcessingService.rotateAndReprocess` now picks the best S3-backed source via the new `pickRotationSource()` helper and only writes back to `originals/` when the original is still present (purged assets stay purged — we don't resurrect bytes retention deliberately dropped). `FileStorageService.rotateImageLeft` keeps a defensive 410 only when no S3-backed source exists at all (unreachable for any DONE asset). `GalleryItem.vue#canRotate` now always true; `originalAvailable` flag retained on `FileInfo` for any future "download original" UI.

### Testing
- [ ] Shorten retention to 0 in staging, run the CronJob manually (`kubectl create job --from=cronjob/...`), verify originals are gone but gallery still serves derivatives.
- [ ] Idempotency: re-run the runner immediately after a successful sweep — eligibility query returns 0 rows because every purged row now has `file_path IS NULL`.
- [ ] Crash recovery: kill the runner between the S3 DELETE and the DB UPDATE for a row → next run sees the row again, S3 DELETE is a no-op, DB null-out completes.

### Risk
Irreversible deletion. Hence the conservative 7-day default, `dryRun: true` in chart values for first-run validation, the `originals/` prefix filter (legacy local-disk paths are deliberately out of scope), and the per-row TX that limits blast radius if the eligibility query ever returns something it shouldn't.

---

## Gap 8 — Slideshow audio to S3 + backend PVC unmount (Phase 3 follow-up, landed 2026-04-29)

Not in the original plan; surfaced when verifying the photo migration was complete and discovering `SlideshowRecordingService` still wrote durably to `${uploadDir}/recordings/`. That blocked the goal of detaching the backend pod from the durable PVC, so audio was migrated under the same direct-to-S3 model as photos.

### S3 key convention (D-new)

Distinct prefix `audio/{audioFilename}` for migrated/new recordings. Legacy paths `recordings/{audioFilename}` continue to mean local-disk relative path. The `audio_path` column is therefore self-describing — `StoragePaths.isAudioS3Key(path)` is the only routing predicate. Different prefix from photos (which already use `originals/`/`derivatives/`) so the audio migration cannot collide with the photo `recordings/` legacy convention.

### Code

- [x] `StoragePaths.AUDIO_PREFIX = "audio/"` plus `audioKey(filename)` and `isAudioS3Key(path)` helpers.
- [x] `SlideshowRecordingService.saveRecording`: stream → temp file in `{uploadDir}/.audio-tmp/` → `audioReencodingService.reencodeAudio` (ffmpeg needs a local file) → `objectStorage.putFile(audio/{filename}, …)` → delete temp → store `audio_path = "audio/{filename}"`. Legacy disk path retained for non-S3 deployments via `Optional<ObjectStorageService>` injection.
- [x] `SlideshowRecordingService.deleteRecording` is S3-aware: deletes the S3 object when the key is `audio/...`, falls through to the legacy file delete otherwise.
- [x] `SlideshowRecordingController.getRecordingAudio` and `getRecordingAudioByPublicToken`: when `RecordingAudioInfo.storageKey` is set, in-band stream from MinIO with HTTP `Range` forwarded via `objectStorage.openStream(key, rangeHeader)`. Returns 200 / 206 + `Content-Range` as appropriate. (We deliberately did **not** 302 to a presigned URL because MinIO has no public ingress; the API pod must mediate.)
- [x] `RecordingAudioInfo` carries `audioPath` (legacy local) **xor** `storageKey` (S3 backed), mirroring `FileServeInfo`.
- [x] `SlideshowRecordingRepository` gains `countRecordingsOnLocalDisk`, `findRecordingsOnLocalDisk`, `findRecordingsOnObjectStorage` (keyset-paginated).

### Migration & verification

- [x] `RecordingMigrationService` mirrors `MigrationService`: HEAD-before-PUT, atomic per-row flip, idempotent re-runs.
- [x] `RecordingVerificationService` mirrors the photo `VerificationService` hash mode. SHA-256 of `uploads/recordings/{filename}` vs SHA-256 of the MinIO object.
- [x] Endpoints (gated by `storage.s3.enabled`):
  - `POST /api/admin/migrate-recordings`, `GET .../status`, `POST .../cancel`
  - `POST /api/admin/migrate-recordings/verify`, `GET .../verify/status`, `POST .../verify/cancel`
- [x] **Outcome**: 41/41 recordings migrated, hash-verified 41/41 (0 missing, 0 hashMismatch, 0 errored).

### Helm — PVC unmount

- [x] `backend.persistence.mounted` (default `false` in chart values) toggles the deployment's `uploads` volume between `persistentVolumeClaim` and `emptyDir { sizeLimit: 5Gi }`. `enabled` still controls whether the PVC resource is rendered, so the PVC stays defined and Bound while detached from the running pod.
- [x] `backend.persistence.emptyDirSizeLimit` (default `5Gi`) sized comfortably above peak transient footprint (largest in-flight upload + per-job derivative workdir + audio reencode temp).
- [x] **Outcome**: live backend pod runs against `emptyDir` only. PVC remains Bound (30 GiB Longhorn) for byte-level recovery if ever needed; can be cleaned out / `--set persistence.enabled=false`'d later.

### Risk / rollback

`emptyDir` is per-pod ephemeral. A pod restart mid-upload kills any `.multipart-tmp` body Spring is currently staging — same property the PVC had, since per-request multipart was never durable. Rollback is `--set backend.persistence.mounted=true`.

### Follow-up

- [x] **PVC retired (2026-05-03).** `backend.persistence.enabled` flipped to `false` in `values.yaml`; helm upgrade dropped the PVC manifest. PV had `reclaimPolicy=Retain` (provisioned that way 201 days ago) so it went to `Released` rather than auto-freeing — explicit `kubectl delete pv pvc-bcfc2395-…` and `kubectl -n longhorn-system delete volume pvc-bcfc2395-…` chained the cleanup all the way down to Longhorn. 30 GiB freed. The historical-content wipe step from the original plan is implicit in this — no separate `rm -rf` Job needed because Longhorn reclaiming the volume drops the data.

---

## Cross-cutting risks

- **Pi memory pressure during transition**: doing Gap 5 (libvips → less RAM) *before* Gap 1 leaves headroom for running the old `@Async` queue + new dispatcher in parallel during cutover.
- **Migration window for existing data**: thousands of rows × 30 Gi PVC → MinIO takes tens of minutes on a Pi. Idempotent and resumable; run during low traffic.
- **Hibernate `ddl-auto: validate`**: every entity field must match a migrated column or app fails to start. Verify entity-vs-migration before each deploy.
- **Single-node K3s availability**: switch the upload pod's deploy strategy to `Recreate` during the multipart-tmp-dir transition window; back to `RollingUpdate` once writes go to MinIO only.

---

## Cross-cutting test plan

| Layer        | Tooling                                                                    |
| ------------ | -------------------------------------------------------------------------- |
| Unit         | JUnit 5 + Mockito for new services                                         |
| Integration  | Spring Boot Test + Testcontainers (MariaDB 11, MinIO, tusd)                |
| iOS unit     | XCTest with mocked URLSession                                              |
| iOS manual   | Airplane-mode toggle, force-quit, OS-reboot-during-upload                  |
| E2E          | Existing web flow extended with admin-status check (`GET /api/assets/{id}/status` reaches DONE) |
| Load         | K6 script: 200 concurrent uploads of 50 MB JPEGs against staging Pi        |
| Chaos        | `kubectl delete pod` mid-processing (Gap 1, Gap 2b) and mid-upload (Gap 3) |
